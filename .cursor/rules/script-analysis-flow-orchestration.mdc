---
description: Script analysis flow - parallel fan-out after asset index (Subject Index)
globs: frontend/src/pages/editor/components/ScriptEditor.jsx,backend/app/services/script_analysis_flow/**,backend/app/api/endpoints.py
alwaysApply: true
---

# Script Analysis Flow Orchestration

## Core rule

**Scene orchestration (`scene_markdown`) and asset design (`asset_design_*`) both depend on the asset list (Subject Index / `assets_extraction`).**

After Stage 2.1 completes and the Subject Index is persisted, trigger **in parallel**:

1. **Scene orchestration** — `scene_markdown` (Stage 2.2; may fan out per scene)
2. **Asset design** — three concurrent categories:
   - `asset_design_character`
   - `asset_design_prop`
   - `asset_design_environment` (environments, posters, covers)

The two main branches run in parallel. The three asset categories also run in parallel (`Promise.allSettled`). Do **not** wait for scene orchestration to finish before starting asset design.

## Topology

```
Stage 1: script_optimization
        |
Stage 2.1: assets_extraction -> persist Subject Index
        |
   +----+----+
   |         |
scene_markdown    asset_design_character --+
(per-scene OK)    asset_design_prop         +-- concurrent
                  asset_design_environment --+
        |                    |
   each scene import         |
   to workspace              |
        |                    v
        +---- wait for env visual assets ready ----+
                            |
                   storyboard_generation per scene
                   (same as scene page 生成分镜;
                   stage3_auto_start.storyboard_generation;
                   do not wait for all scenes / character / prop)
```

## Registry (source of truth)

File: `backend/app/services/script_analysis_flow/registry.py`

- `assets_extraction.fan_out`: `scene_markdown`, `asset_design_character`, `asset_design_prop`, `asset_design_environment`
- `assets_extraction.fan_out_mode`: `parallel_after_complete`
- `storyboard_generation.depends_on`: `scene_markdown`, `asset_design_environment`
- `stage3_auto_start` toggles auto-start per category and storyboard

## Frontend (`ScriptEditor.jsx`)

| Entry | Behavior |
|-------|----------|
| `executeAdvancedAnalysis` | After 2.1: `Promise.allSettled([runSceneOrchestrationBranch(), runAssetDesignBranch()])` |
| `runPostImportSceneSubjectPipeline` | character / prop / environment LLM calls concurrent; when env subtask succeeds, flush pending storyboard queue |
| `handleRestartStage2` | Full scene branch + asset branch in parallel after 2.1 |
| `tryResumeAnalysisFromExistingArtifacts` | If index exists but scenes missing: scene + assets parallel; if only subjects JSON incomplete: assets only |
| Per-scene import success | After workspace import: queue/kick `kickoffStoryboardForImportedScene` (gated on env visual assets ready) → `generateSceneShots` + `applySceneAIResult`; update diagnostics `storyboardTaskProgress` |

API: `runScriptAnalysisFlowAnalyzeNode` -> `POST /prompts/scene-analysis/flow/run-node`

## Backend

- `run_scene_analysis_flow_node`: runs one node; does **not** auto fan-out after `assets_extraction` (frontend orchestrates)
- `scene_markdown`: `_run_scene_markdown_node_per_scene` — per-scene parallel orchestration and import
- Asset design nodes: `analyze_scene` per category, separate persist

## Constraints when changing code

1. New stages that consume Subject Index: add to `assets_extraction.fan_out`, parallel with `scene_markdown`
2. Resume/restart paths must match main path concurrency
3. Never `await` scene orchestration before starting asset design after 2.1
4. Asset design with `explicitSubjectIndexText` / `parallelWithScenes: true` must not require scene import first
5. After each scene imports to workspace, start that scene's storyboard only when **ENV** asset design is ready (`asset_design_environment` ok / imported, or no ENV rows / env auto-start off); queue as `waiting_env` until then. Do **not** open the gate when character or prop finishes. Arm the ENV gate (`environmentAssetDesignPendingRef`) before parallel scene+asset branches so early imports cannot use stale episode JSON. End-of-pipeline only residual-kicks scenes not yet started once the ENV gate is open.
6. Storyboard prompt injection (`# Scene Subject Image Prompts (CN)`): **ENV-only** for the current scene's linked environments — do **not** inject CHAR/PROP `generation_prompt_cn`. Subject Index may still list CHAR/PROP/ENV for naming.
7. Manual storyboard rerun (`force` kickoff) must still verify each scene's linked ENV exists in the entity library with `generation_prompt_cn`; block/skip scenes that fail this check.

## Scene orchestration asset annotation (prompt contract)

- **Stage 2.2 定位（强制）**：`scene_markdown` / `beats_generation` **仅为** Stage 1 成稿 + Subject Index 的工程结合与转译——零丢失落表、Index 名称标准化；**禁止**改情节、改对白、改建置、补实体、推断填洞或回填上游缺口。
- **Subject Index 白名单 / 命名绝对锁**：场景编排（Stage 2.2 / `scene_markdown`）与资产设计（Stage 3 / `asset_design_*`）生成时，凡实体名（`CHAR:`/`ENV:`/`PROP:` 方括号名、JSON `name`/`name_en`/`base_name_en`、`visual_dependencies` 内名称）**必须**与 Subject Index 对应行 `subject_name_zh` / `subject_name_en` **逐字符完全一致**；**禁止任何形式的修改**（润色、规范化、翻译、缩写、同义替换、标点/空格/大小写修正、合并拆分等）。**禁止**对 Index 无行实体套用任何类型前缀或自创格式（`EXTRA:`、`LOCATION:` 等）；Index 外语义只保留 Stage 1 自然语言并在 `{覆盖核销}` 标缺口。
- **CHAR/PROP 衍生族证据匹配（强制）**：落位每个角色/道具时，须据 Index `base_entity`/`dependency_reference` 判别是否存在衍生关系实体（有依赖链即衍生关系）；同族多行时**仅**按 Stage 1 本 Beat **明文证据**匹配对应行；无明文证据则落基础版并标核销备注；**禁止**推断未写态、禁止改写剧情以迁就 Index、禁止错套与明文不符的衍生态。
- **ENV 仅整体切换（强制）**：`ENV:[]` **只**标注 Index `environment` 整场行，且须为 Stage 1 **已写明**的主环境或 `{N}度…`/状态衍生；**禁止**补 Stage 1 未写衍生 ENV；留在 ENV 空镜、**未**升格为 `PROP` 的固定陈设/建筑构件**禁止**单切为 `ENV:[会议桌]` 等；环境只能整场切换，不得把环境内部件工程化为局部 `ENV:`。

## Environment reference model (prompt contract)

- **剧本服务（Stage 1 强制）**：所有环境设置须**因剧本而生、为剧本而用**——先读本场剧情需求再规划【主环境】/【衍生环境】；禁止脱离剧本的装饰性扩写或无 Beat 使用的冗余衍生；**服从衍生环境最小化**——≤90° 及运镜可解决的**非反打**变化不另建衍生行；**任何反打必须切 ENV**。**剧本活动空间（强制）**：主环境须明文【活动空间】（主舞台/表演区｜动线｜站位承载｜出入画口）；衍生行加`活动适配=…`；空间须可站可动可出入可分组，与本场 Beat 站位/逃窜/跪拜等匹配，禁只堆陈设无表演区。**Stage 2.1 继承**：主环境行写 `activity_space:`、衍生行写 `activity_fit:`（摘抄上游，禁臆造；缺则 `upstream_missing_activity_space` 回流）。**中英同义去重（Stage 1/2.1）**：输入中英同义并列时只保留项目信息 `project_language`/`Language` 侧；专有名词与有动机异语除外。**原文逐字核销（Stage 1 最高）**：原剧本每一处文字须落入优化稿可检索落点；对白/OS/可见字（项目语言侧）**逐字**保留（拆句可拼接还原全文）；叙述/动作/场景逐信息单元零丢失；专名逐字；**`△俯拍/航拍`与`【空境/空镜：…】`等建立空镜须落入可拍 Beat 入戏**（禁只挂【主环境】/场头；禁因空镜重述限定删除）；禁概括吞并或删半句。Part1 须交【原文逐字核销】；无落点=整场失败。与中英去重：只核销保留侧。**服化道/环境初始态锁定（Stage 1 §7 最高）**：服化道三项与【主环境】基准须**严格按原文初始建立描述**（场头/空境/开场/入场初态）；**禁止**把剧情发展后的狼藉、废墟、战损衣态、毁损道具等回填进初始摘要或主环境基准；过程变化进 Beat；跨拍可持续结构/氛围改写→状态衍生；改写后空间实质异质且**长时间停留**→新 Scene 或另立主环境（视停留时长）；异地→按情节异地新建。Stage 2.1/3 基础版与主环境四向拼图继承初始态，剧变只落已声明衍生行。
- **主环境**：基准定义（俯视360+0度坐标轴；仰视360可选；**=原文初始建立态，非剧情终态**）；**0 度轴锚定优先级（唯一序）**：①上游已给基础/0度视角 → 继承；②否则 = 本场首个非航拍全局环境视角（Wide/Full/Master/Two Shot/LS 等眼高/地面级；**排除** Insert/CU/ECU/MCU/特写吸睛**与**航拍/高位俯瞰）的背景方位；③首 Beat 特写/航拍时尚无全局 Beat → 规划层可先以主路/主门/正堂/中轴线为 0° 背景半空间预声明并标「0度轴锚定 Beat」，待全局 Beat 锁定；**0 度方向 = 该视角的背景方位**（远端/背景半空间；规划 0° 扇区实体时优先挂主路/主门/正堂/中轴线）；**可不写 前景/中景/背景**，须写 **0/90/180/270 四向+中心**（**四向包络 = 剧情与环境描述的法定边界，不得向外扩张**；**严禁对向串写**——0° 不许描述 180° 内容，90° 不许描述 270° 内容，反之亦然；对向实体只写在对向自己的段落），头尾双锚、固定实体清单（**实体唯一性优先**：原则上禁止物体重复，门/窗默认全场唯一，除非剧情明文需要；确须同类多件时先写个数→逐具具名+区分特征，并写清**分边个数/朝向/同侧顺序**（禁笼统「有椅子」）；**实体配套性**：功能成套须成套写入，如桌↔椅、床↔被褥/枕、杯↔盏/托；**含本场角色出入画所需具名出入口**）；Stage 3 输出**四向拼图**（16:9，2×2，眼高机位，非俯拍）；`referenceable:No`；**是未改写氛围下该主环境族全部视角衍生的默认生图参考源**；若已有仍在延续的状态/氛围衍生，则该状态行为当前空镜基准。
- **情节异地新建环境 + 闪回 Scene 分档（Stage 1 §7/§11）**：情节确认**不在当前主环境**时须新建【主环境】+对应衍生族（原文具名或剧情可核销异地意图均可；优先同环境复用）。**快速闪回/蒙太奇**：不新起 Scene，同场多主环境（每落点各一主+`0度`）；Beat 触发=`闪回切主环境|蒙太奇切主环境|回切当下主环境`（禁标反打）。**较长回忆片段**：须新起独立闪回 Scene（自有主环境族）。禁用当下衍生冒充异地；禁长回忆硬塞同场、禁快闪/蒙太奇误拆多 Scene。Stage 2.1 逐块提取；Stage 3 各主各自四向拼图、衍生只挂所属主。
- **未落环境实体清单（Stage 1 §11 强制，紧接【主环境】后）**：本 Scene 全场明文提到、且须在 Beat 建置段做落位安排的实体盘点（道具/静物/可交互物件/载具等）；只列具名+须建置依据，不写七要素落位；角色/群演归【Scene实体覆盖】。与开场预排布、各 Beat 建置交叉核销；清单外禁止中途凭空新增。多主环境时可标注归属主环境。Stage 2.1 可作 PROP 线索，**禁止**据此另建 ENV 行。
- **实体唯一性（最高硬约束，Stage 1 §11⑤ / Stage 3；防生图识别混淆）**：各向实体描述时**原则上禁止物体重复**——默认每种功能实体**在所属主环境内**仅 1 具；**门/窗尤严，通常只有一扇**；除非剧情明确需要多门多窗/成套多件，禁止为填满四向而复制同名同功能实体。确须多件时才走「先写总个数→逐具具名+区分特征」。**同质多件排布（Stage 1 §11⑤-b / Stage 3；椅/凳等）**：≥2 具时须写清总个数、**桌各边/各向几张**、逐具朝向、同侧摆布顺序；**禁止**笼统「有椅子」。Stage 3 衍生 `generation_prompt_cn` §B 须要求排布**完全参考所属主环境四向拼图参考图**，不得重排改数。Stage 3 美术增补**禁止**擅自把上游单门/单窗扩成多门/多窗。
- **实体配套性（强制，Stage 1 §11⑤-c / Stage 3）**：功能上成套/依附使用的实体须成套写入固定实体清单——典型 **桌↔椅/凳、床↔被褥/枕、杯↔盏/碟/托**（及笔砚、灯座等同类）；禁止只写主件漏配件；例外仅限剧情明文残缺/空置/异形制。Stage 3 须完整继承配套关系，禁只画主件或擅自拆套。
- **固定实体清单零遗漏（强制，Stage 1 §11⑥/Stage 2.1/Stage 3 三段共同责任）**：本 Scene 剧本提及的**全部**实体（不论道具级/装饰级/背景级/一次性露出）均须写入主环境固定实体清单，不可因"次要/不够重要"省略；**是否升格为独立 `PROP` 与 Stage 1 是否收录入主环境清单无关**——Stage 2.1 按 `ENV/PROP` **XOR 互斥**裁定：升格为 `PROP` 者从 ENV 剥离；未升格者仍须保留在 `ENV` 固定陈设字段；**禁止**同一实体既在环境描述中又被提取为道具。Stage 3 描述时同样须逐一转译，禁止在环境与道具两侧同时消失。
- **空间均衡性（强制，Stage 1 §11⑦ / Stage 3 §3.1.2）**：固定实体布局与美术增补须防过挤（主舞台/动线阻塞）与过空（四向空洞棚拍感）；次要陈设分散落于边缘/背景向；增补偏空才增、偏挤则控杂。**舞台中区禁增障碍物**：上游未明确且非剧情必须时，禁止在主舞台/中间表演空间额外增加家具类立体障碍；仅允许地毯等不影响行动的贴地实体或剧情硬核销实体。
- **角色四向分布（Stage 1 §12 原则）**：无剧情站位冲突时，画内角色原则上按主环境 **0°/90°/180°/270°** 均匀分布；**礼仪/尊卑/礼法优先于均匀**，有剧情站位需求时服从剧情。**人群分析与四向分组**见下「单 Beat 3 人以上分组」。
- **角色环境速查（Stage 1 §12 强制；并入【观察视角与空间建置】）**：目标=锁定每角色/组主场衍生 ENV + **世界扇区** + **F** + 姿态/锚。Scene 级速查表 + 每拍末`速查更新`（=终态）为跨拍真源。落笔序：**世界态→按当前 ENV 之 N 查表→【建置】【入戏】**；切角必查换算表。F 映射：F≈N→背对/侧背面；F≈N+180→正面（禁侧背面）；F≈N+90→右侧面+面向画右；F≈N+270→左侧面+面向画左。建置禁 `F≈`；速查扇区查表须≡建置层位（有走位则入戏链闭合）。≥2 人同层须分画左/中央/画右。`ENV变=是` 填`视角换算台账`；反打禁换主环境名。**建置防错乱五锁**：时序｜投影｜朝向｜ENV名｜集合。
- **前中后景及左中右相对参照公约（最高硬约束）**：凡写方位，一律以镜头朝向看——并在成稿必须明确标示距离，如前景(离镜近处)=离镜头最近，背景(远处)=最远，并区分画面的左边和右边。【建置】【入戏】除环境名外【严格禁止】出现面向0度/背向180度/0°侧/躯干面向N°侧等世界度数推演；度数世界态**只**进每拍`速查更新`与 ENV 变时`视角换算台账`。绝对禁止只写“中景”或“前景”而不带说明括注！躯干朝向等必须转换为“背对镜头、画面左侧、背景远处、前景(离镜近处)、面向画右、面向{具名对象}”等镜头相对词；**换镜后必须按新 N 重投影，禁粘贴上拍画面词**。禁止以世界前墙/后墙定义层位。**朝向≠人所在层位**；**扇区≠景深**；机位后方不可见禁写背景。**世界态优先（切视角根治）**：真源=`扇区+F+姿态+锚`（跨拍继承）；画面词=投影；`ENV变=是` 且无走位时未动者画面层位/左右/正背面**必须相对上拍变化**，否则=未查表失败。
- **入画径向↔正背面硬锁（强制，Stage 1 §12）**：经具名门/出入口入画时，出入口相对本镜层位须与行进径向、终态正背面闭合——**背景门入 → 朝镜头逼近 → 终态正面/¾、门在身后**；要背影进深处 → 门须近端/画外或切对向 ENV；禁裸写「面向…深处」无相对门锚；禁同 ENV「背景门 + 前景背对 + 面向深处」。
- **Beat 完全独立自洽与建置内容完整继承（最高硬约束）**：即使当前Beat的机位、人物站位和上一Beat【完全相同】，也【绝对禁止】在建置段使用“同上”、“承接上个Beat”、“ENV不变”之类省略。必须把上一Beat终态（结束态）所有的角色方位、朝向、姿态【一字不落】地直接复制书写到当前Beat的建置中（作为本Beat基础）！每个Beat抽出来都必须自带完备的视觉定位，禁任何人省略绝对左中右和远近坐标。
- **Scene 不适配须拆 / 场景描述融入 Beat（Stage 1 §9）**：输入场次若含明显不适合当前主环境的动作/对白，须另拆 Scene（禁硬塞本场）；闪回分档见§7——**快速闪回/蒙太奇**同场多主不新起 Scene；**较长回忆**须新起闪回 Scene。原文场景文学/氛围描述、`△俯拍：…`、`【空境/空镜：…】` 建立句须融入 Beat 入戏（优先独立首拍/吸睛；无人戏亦可；航拍不作 0°轴），禁只留场头或【主环境】文学说明。
- **单 Beat 3 人以上分组（Stage 1 §12）**：单 Beat/分镜同框超过三人须分组（每组≤3），各组分布到不同环境角度（**优先 90° 倍数**衍生 ENV），分 Beat 分批建置；禁止单拍硬塞全场。**环境物理隔离**：角色较多时分组后须在主环境布局上适当隔离——同场入座/落位超过四人优先两桌/两区（或等价分席），禁无依据超四人单桌硬塞。**群演延迟入画**：群演/龙套/无具体剧情功能者尽量勿开场即入画，待合适 Beat 用动作经具名出入口入画。**一旦入画后守恒（强制）**：已入画群演/角色须后 Beat 逐拍承接落位，或明文写出经具名出入口的可拍出画过程；**禁止**无交代从建置/`{Beat终态画内实体}` 静默抹除——群演不豁免。**群演随视角建置变化（强制）**：`ENV变=是`/切角时，群演簇/龙套/手下须按新观察轴**重判并重写**画面层位与正背面，禁止粘贴上一拍画面句或因「只是龙套」忽略不建置；同 ENV 未切角时才可原样抄写。**群演分组与主人物空间距离（强制）**：群演按分组原则自成气氛/背景组，**尽量**与主要人物拉开空间距离（不同扇区/更远层位/边缘区）；禁止无交互依据贴肩贴桌抢主舞台；近身仅限剧情明文交互。
- **角色建置含进出场 + 后 Beat 守恒（Stage 1 §1 最高硬约束）**：建置须把进出场纳入闭环；已在场角色/宠物/群演簇/具名手下后 Beat **二选一**——一直留在画面（可退后景/虚焦一角仍须可核销），或入戏写合理出入画过程链至可拍终态；切角/景别收紧/ENV切换≠出画。**本拍 `{Beat终态画内实体}` ≡ 本拍建置具名集合**（禁终态写保镖簇/群演而建置无落位）；且终态须逐名落入**下一拍**建置——禁止切换说明列多人、本拍/下拍建置只剩主锚。**入戏方位硬锁**：`对画左/画右的{名}` 须与建置落位左右一致；对象居中则禁写画左/画右。
- **身体姿态必填（Stage 1 §1/§12 最高硬约束，全场景）**：角色建置须逐人明文写清当前身体姿态——站/坐/躺/跪/蹲/趴/倚靠/骑乘等；禁止只写位置/朝向而省略姿态；座/躺/倚须挂具名承托面，姿态变更须入戏过渡并跨 Beat 承接。
- **开场预排布静物/道具（Stage 1 §1/§12/§17 最高硬约束）**：建置规划须通读全场、以前瞻【未落环境实体清单】为盘点表，**不得只按本 Beat 戏份取舍**。无自主入画能力的静物/道具（桌面物、陈设级可交互件等）**默认开场即在场并写入开场建置落位**——即便开场尚无其剧情交互；**禁止**等到有戏份的 Beat 才突然出现。仅当清单/【Scene实体覆盖】明文「待入画」且有携入/投掷/容器开启暴露等可拍过程链时方可晚到。与「道具不得凭空消失」对称：先预排布、后承接、禁凭空进出。
- **并排/队列中间位必标（Stage 1 §1/§12）**：建置涉及并排、队列、一字排开、纵列、夹中等使 ≥3 具名角色成列/成排时，中间位具名人物须明文标槽——横向写左/中/右，纵向写前/中/后；禁止中间人只写「夹在甲乙之间」而无 `中`。
- **他人目光默认朝向说话人（Stage 1 §12 对白咬合）**：某角色画内发言时，同框其他应可见角色目光/视线默认落在该说话人；仅当剧情刻意表达冷漠、无视、回避、分心等时才可例外，且须明文写清例外情绪与实际视线落点。
- **有桌有凳时的站坐安排（Stage 1 §12 强制）**：主环境含桌且配套凳/椅等可坐具时，在姿态必填之上另须裁定站或坐（座姿挂具名椅/凳）；判定顺序：剧情强制 → 礼仪/尊卑/座次 → **无特别限制时优先安排坐下**；禁止无依据全员围桌站聊。**剧情与礼仪允许时**：尽量按桌各边/各向有序分散坐定，并逐人写清相对桌的哪一侧/哪一头、坐哪把具名椅、该向位序；禁止只写「坐下/围桌」而无桌向与位序。同场入座超过四人时优先两桌/两区隔离（见「单 Beat 3 人以上分组」）。
- **角色出入画出入口（强制，Stage 1 §11⑧ / Stage 3 §2.2）**：环境设计阶段须为角色入画/出画预置**具名可通行出入口**（门/门洞/走廊口/巷口等）；Beat 出入画须挂靠该具名出入口，**禁止**只写「从右边进入」或从实墙凭空出入；**经可开合门/窗时须按开向与门轴侧写过程链**（开门→人通行→带上门→默认关闭终态；剧情明文留门例外），禁跳步只写「推门进来」；Stage 3 须可视化出入口并保留通行净空。入画后正背面另服从上条「入画径向↔正背面硬锁」。
- **运动终点必具体（强制，Stage 1 §12）**：角色跑/走/冲等运动**禁止**只写「跑向某处/往前冲/朝他跑去」；须写清终点——具名地点/ENV 结构、具名人旁（方位+距离）、经具名出入口出画外、或画内具体三轴+具名参照落位。
- **特效对实体作用（强制，Stage 1 §8 / Stage 3 §2.5）**：特效/域场须写清作用范围 + 哪些具名固定实体受何种可见作用（灼裂/倾侧/符纹覆盖等）+ 未受影响实体保持原态；禁止只写「能量弥漫」而无实体落点；Stage 3 状态/特效衍生 §C 须逐实体转译。
- **不可见驱动力可视化（强制，Stage 1 §8）**：剧情出现灵气/能量/气/内功/真元/罡气/魔力/念力/力场等本不可见却实际驱动运动或产生影响的力量时，须在对应 Beat 入戏写成可拍可见形态（形态+轨迹+作用关系）；禁止只写「运功/运气」或只写结果而无介质画面；小规模可同拍压缩，大规模叠加特效六相。
- **可拍 ENV**：全部为 `{N}度{主环境名}` 视角衍生（**含强制 `0度{主环境名}` Master，观察轴按 0° 轴锚定优先级；多主环境时每主各一 `0度`**）；Beat 只指向衍生行；**一 Beat 一 ENV**（ENV 名变化须另起 Beat）。**衍生环境最小化（Stage 1 强制）**：仅当偏转 **>90°**、越轴、门窗内外/POV/状态·特效等结构切换时另建衍生行；**≤90° 及运镜类变化**（特写/Insert/CU/**非反打**过肩构图/焦点人物转移）**沿用父观察侧衍生 ENV，禁止另建**。**任何形式的反打必须切 ENV（最高硬约束）**：正反打、过肩后看对手的 OTS、对白轮次读对方——**不论偏转是否 ≤90°**，均须 `ENV变=是` 并切换可读角衍生 ENV；禁止反打仍挂上一拍同 ENV；**反打不得跨主环境名**（跨主=闪回/蒙太奇切主环境，非反打）。**对白双方异排异向例外（强制）**：画内双人对话**不同排**或**身体朝向夹角 >45°**（45° 内视为同向）时，须为**各说话人各备**背景衍生 ENV 并在轮次间合理切换。**特写/Insert/CU/ECU/MCU 仅景别收紧、观察轴未变且非反打时沿用父观察侧衍生 ENV，禁止另建特写专属衍生行**。**特例（绝对必要切视角）**：若须给**背对/侧背不可读**的角色做面部特写等，必须先切至其可读角衍生 ENV，并在建置段明文标注【上一 ENV → 触发 → 本 ENV={可读角名}】、完成落位重建后，才可继续写特写/表情/口型近镜；禁止同 ENV Push In 硬露脸。
- **参考链**：无状态改写时，视角衍生（含 `0度`、N≠0）生图 `visual_dependencies` / `reference_env` → **`ENV:[所属主环境名]`**；**禁止**同基准内角度互挂（如 180° → 0°）；**禁止**闪回主环境衍生挂当下主环境。**状态/氛围衍生（强制）**：跨 Beat 可持续的结构/域场改写，或重大氛围/风格切换（光线、色彩、天地异象等）另建 `{主环境}_{状态}`；其 `reference_env` → 所属主环境或紧邻上一状态。**衍生的衍生（强制）**：状态确立并 `continue` 后，后续视角/再状态须以该状态衍生为被依赖基准（`reference_env` / `visual_dependencies` → **`ENV:[状态衍生名]`**），命名如 `{N}度{主环境}_{状态}`；禁止状态切换后仍跳回所属主环境。
- **跨阶段职责边界（强制）**：**Stage 1/2.1 只声明轻量角度清单**——衍生环境名、`view_angle_from_main`、触发原因（一句话，OTS/反打须含两步确认结论 `N_对手`→`(N_对手±180)%360`）、`spatial_axis`、`lens_profile`（Master/建置=`Wide`；OTS/反打=`Standard`，禁广角）、`axis_crossing`（180°反打=`PlannedReverse`；越轴=`MotivatedCross`）、`empty_view_delta`（可选一句话）；**不写**该衍生的 0/90/180/270 四向具名实体描述或前景/中景/背景 空镜层次。**Stage 3（entity_design_environment_and_poster.md §2.5）是四向对位与美术设计的唯一执行方**：依据 Stage 1【主环境】固定实体清单 + 上述角度/触发/轴线元数据独立完成——四向实体用 **【{N}度方向】** 自然语言，**{N} 仅指本衍生观察轴顺时针方位**（与主环境同名度数不是同一参照），禁键值标签式；**严禁对向串写**（0°↔180°、90°↔270°互不点名对面扇区实体，禁止「不可见{对向实体}（机位后方）」）；**邻向入境硬约束（强制）**：相邻两向内容允许入境但**只能有小部分可见**——成稿只写画幅贴边斜切/半截侧缘/窄条透视（约≤10%–15% 画幅宽贴边条带）；禁邻向大半入画、完整正面/整扇门窗/整列柜体或写成第二主景；**四向具名实体须与主环境固定清单逐字一致、同等具体度**，逻辑严格匹配，仅本扇区内遮挡/方向性物体锚点朝向/正反面/左右序位可变；**可数同类实体（椅/杯/窗/柜等）总个数、分边/各向个数、逐具朝向与同侧摆布顺序、逐具区分特征跨主/衍生须一致**（且服从**实体唯一性优先**：默认不重复，门/窗通常仅一具；衍生提示词须显式要求排布完全参考主环境参考图）；**主环境须声明头尾双锚**（各从四向固定实体择一，分居 Viewing Direction 轴线两端，由 Stage 1 主环境声明）；成稿**优先挂可见头/尾锚**，首选不可见时改挂对向锚；**固定实体世界朝向跨衍生须一致**（状态/特效衍生或明示受力移位除外）；**环境描述/生图词禁人物（最高）**：主/衍生 `environments[]`=`纯空镜`——无人名/人称/站位/交互，**亦禁**「用于某角色干什么」的用途叙事；上游 purpose/activity 仅规划参考，不得写入 `generation_prompt_cn`（见 entity_design §2.6 / common Clean Plate）；**衍生环境 前景/中景/背景 每层每实体须写清上/中/下**（主环境可不写 前景/中景/背景）；**方向性实体类目见 Stage 1 §11「方向性实体参考清单」**。**成稿 vs 内部推演**：转角对照、镜像映射、分层推演、交叉校验、轴线回转公式等仅作内部推演；**成稿方向性物体须锚定四向固定实体（头尾双锚优先）**，头尾均不可见时才用相对镜头/Observer View 视角表述，**禁**成稿写度数回转/公式/轴线回转向；**禁**主环境/转角对照/继承/映射等回指（细则 entity_design_environment_and_poster.md §2.5）。

## Prompt files

- 2.1: `skills/scene_analysis_feature_stack/scene_planning_2_1_assets_extraction.md`
- 2.2: `skills/scene_analysis_feature_stack/scene_planning_2_2_beats_generation.md`
- 3: `entity_design_character.md`, `entity_design_prop.md`, `entity_design_environment_and_poster.md`
- Stage 1 backfill: `project_visual_backfill.color_spectrum` — 基于 `borrowed_films` 的文学级主冷暖方向（四选一：冷调主导|暖调主导|冷暖对比|同温层次）；Stage 3 §1.5 须以其为首要色谱推导输入
