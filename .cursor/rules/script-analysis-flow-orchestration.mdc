---
description: Script analysis flow - parallel fan-out after asset index (Subject Index)
globs: frontend/src/pages/editor/components/ScriptEditor.jsx,backend/app/services/script_analysis_flow/**,backend/app/api/endpoints.py
alwaysApply: true
---

# Script Analysis Flow Orchestration

## Core rule

**Scene orchestration (`scene_markdown`) and asset design (`asset_design_*`) both depend on the asset list (Subject Index / `assets_extraction`).**

After Stage 2.1 completes and the Subject Index is persisted, trigger **in parallel**:

1. **Scene orchestration** — `scene_markdown` (Stage 2.2; may fan out per scene)
2. **Asset design** — three concurrent categories:
   - `asset_design_character`
   - `asset_design_prop`
   - `asset_design_environment` (environments, posters, covers)

The two main branches run in parallel. The three asset categories also run in parallel (`Promise.allSettled`). Do **not** wait for scene orchestration to finish before starting asset design.

## Topology

```
Stage 1: script_optimization
        |
Stage 2.1: assets_extraction -> persist Subject Index
        |
   +----+----+
   |         |
scene_markdown    asset_design_character --+
(per-scene OK)    asset_design_prop         +-- concurrent
                  asset_design_environment --+
        |                    |
   each scene import         |
   to workspace              |
        |                    v
        +---- wait for env visual assets ready ----+
                            |
                   storyboard_generation per scene
                   (same as scene page 生成分镜;
                   stage3_auto_start.storyboard_generation;
                   do not wait for all scenes / character / prop)
```

## Registry (source of truth)

File: `backend/app/services/script_analysis_flow/registry.py`

- `assets_extraction.fan_out`: `scene_markdown`, `asset_design_character`, `asset_design_prop`, `asset_design_environment`
- `assets_extraction.fan_out_mode`: `parallel_after_complete`
- `storyboard_generation.depends_on`: `scene_markdown`, `asset_design_environment`
- `stage3_auto_start` toggles auto-start per category and storyboard

## Frontend (`ScriptEditor.jsx`)

| Entry | Behavior |
|-------|----------|
| `executeAdvancedAnalysis` | After 2.1: `Promise.allSettled([runSceneOrchestrationBranch(), runAssetDesignBranch()])` |
| `runPostImportSceneSubjectPipeline` | character / prop / environment LLM calls concurrent; when env subtask succeeds, flush pending storyboard queue |
| `handleRestartStage2` | Full scene branch + asset branch in parallel after 2.1 |
| `tryResumeAnalysisFromExistingArtifacts` | If index exists but scenes missing: scene + assets parallel; if only subjects JSON incomplete: assets only |
| Per-scene import success | After workspace import: queue/kick `kickoffStoryboardForImportedScene` (gated on env visual assets ready) → `generateSceneShots` + `applySceneAIResult`; update diagnostics `storyboardTaskProgress` |

API: `runScriptAnalysisFlowAnalyzeNode` -> `POST /prompts/scene-analysis/flow/run-node`

## Backend

- `run_scene_analysis_flow_node`: runs one node; does **not** auto fan-out after `assets_extraction` (frontend orchestrates)
- `scene_markdown`: `_run_scene_markdown_node_per_scene` — per-scene parallel orchestration and import
- Asset design nodes: `analyze_scene` per category, separate persist
- **Scene split (strict pair walker)**: `parse_scene_units_from_markers` / FE twin require `[SCENES_BLOCK_START]`…`[SCENES_BLOCK_END]` (first END) and paired `[SCENE_START:{id}]`…`[SCENE_END:{id}]` (ID required, same id both sides). **No** bare `[SCENE_START]`/`[SCENE_END]`, no 50-char drop filter, no single-scene soft collapse on parse failure. Stage 1 prompt (`scene_planning_1_script_optimization.md` §10) **must** stay in lockstep with this contract.

## Constraints when changing code

1. New stages that consume Subject Index: add to `assets_extraction.fan_out`, parallel with `scene_markdown`
2. Resume/restart paths must match main path concurrency
3. Never `await` scene orchestration before starting asset design after 2.1
4. Asset design with `explicitSubjectIndexText` / `parallelWithScenes: true` must not require scene import first
5. After each scene imports to workspace, start that scene's storyboard only when **ENV** asset design is ready (`asset_design_environment` ok / imported, or no ENV rows / env auto-start off); queue as `waiting_env` until then. Do **not** open the gate when character or prop finishes. Arm the ENV gate (`environmentAssetDesignPendingRef`) before parallel scene+asset branches so early imports cannot use stale episode JSON. End-of-pipeline only residual-kicks scenes not yet started once the ENV gate is open.
6. Storyboard prompt injection (`# Scene Subject Image Prompts (CN)`): **ENV-only** for the current scene's linked environments — do **not** inject CHAR/PROP `generation_prompt_cn`. Subject Index may still list CHAR/PROP/ENV for naming.
7. Manual storyboard rerun (`force` kickoff) must still verify each scene's linked ENV exists in the entity library with `generation_prompt_cn`; block/skip scenes that fail this check.
8. Changing scene-marker parse rules requires updating Stage 1/2.1/2.2 prompts in the same change (ban bare markers; require `EPxx_SCyy` pairs).
9. Before Stage 2.1 / 2.2 inject optimized script: strip paired `────【Beat切换说明】────`…`────【Beat切换说明结束】────` only (`strip_beat_transition_notes_from_script` / FE twin). Never greedy-strip without end marker. Stage 1 must emit both markers; 2.1/2.2 must not require or restore that block.
10. **软性外形决定因素**：Stage 1 须原词保真身份、特殊用途、地位及剧本明确写出的外形评价词（高贵/精致/漂亮/性感/绝美等）；Stage 2.1 完整写入 `entity_attributes`（`identity`/`purpose`/`social_status`/`appearance_cues`，评价词禁改成「好看」）；Stage 3 转译进定妆/空镜/道具生图。无原文评价词禁臆造。
11. **身份·地位·明文性格锁言行**：主笔与 Stage 1 中角色的**动作、行为、语言**须严格服从身份、地位及原文/大纲已写性格；豪门/高户默认内敛，农家默认直接；无明文性格禁臆造；可拍化/加密微表演不得抹平阶层差或性格棱面（对白仍逐字保真）。

## Scene orchestration asset annotation (prompt contract)

- **Stage 2.2 定位（强制）**：`scene_markdown` / `beats_generation` **仅为** Stage 1 成稿 + Subject Index 的工程结合与转译——零丢失落表、Index 名称标准化；**禁止**改情节、改对白、改建置、补实体、推断填洞或回填上游缺口。
- **Subject Index 白名单 / 命名绝对锁**：场景编排（Stage 2.2 / `scene_markdown`）与资产设计（Stage 3 / `asset_design_*`）生成时，凡实体名（`CHAR:`/`ENV:`/`PROP:` 方括号名、JSON `name`/`name_en`/`base_name_en`、`visual_dependencies` 内名称）**必须**与 Subject Index 对应行 `subject_name_zh` / `subject_name_en` **逐字符完全一致**；**禁止任何形式的修改**（润色、规范化、翻译、缩写、同义替换、标点/空格/大小写修正、合并拆分等）。**禁止**对 Index 无行实体套用任何类型前缀或自创格式（`EXTRA:`、`LOCATION:` 等）；Index 外语义只保留 Stage 1 自然语言并在 `{覆盖核销}` 标缺口。
- **CHAR/PROP 衍生族按场换版（强制）**：Stage 2.1 衍生行须在 `entity_attributes` 写明 `applicable_scenes`（`EPxx_SCyy`，多场`|`分隔）、可选 `activation_beat`，以及 `derivative_kind`/`derivative_trigger`/`activation_evidence`/`variant_delta`/`return_or_continue`。Stage 2.2 落位时：①仅选用 `applicable_scenes`∋本场 `Scene ID` 的衍生行；②再按 `activation_beat`/`activation_evidence`/`variant_delta`∩本 Beat 明文匹配；③`continue` 同场后续拍无冲突时沿用；④无适用衍生→基础版。**禁止**跨场借用未列场景号的衍生、推断未写态、改写剧情迁就 Index、错套与明文不符的衍生态。
- **ENV 仅整体切换（强制）**：`ENV:[]` **只**标注 Index `environment` 整场行，且须为 Stage 1 **已写明**的主环境或 `{N}度…`/状态衍生；**禁止**补 Stage 1 未写衍生 ENV；留在 ENV 空镜、**未**升格为 `PROP` 的固定陈设/建筑构件**禁止**单切为 `ENV:[会议桌]` 等；环境只能整场切换，不得把环境内部件工程化为局部 `ENV:`。

## Environment reference model (prompt contract)

- **剧本服务（Stage 1 强制）**：所有环境设置须**因剧本而生、为剧本而用**——先读本场剧情需求再规划【主环境】/【衍生环境】；禁止脱离剧本的装饰性扩写或无 Beat 使用的冗余衍生；**服从衍生环境最小化**——≤90° 及运镜可解决的**非反打**变化不另建衍生行；**任何反打必须切 ENV**。**同场衍生ENV积极换角（Stage 1）**：切换合理时须按剧情/对白轮次/角色情绪表情改挂已声明不同 `{N}度…`；**同一衍生名连续使用不得超过 3 个 Beat**（可切回，每次连续段仍≤3；合理=覆盖+守轴+可拍，禁无动机越轴/机械轮角）。**剧本活动空间（强制）**：主环境须明文【活动空间】（主舞台/表演区｜动线｜站位承载｜出入画口）；衍生行加`活动适配=…`；空间须可站可动可出入可分组，与本场 Beat 站位/逃窜/跪拜等匹配，禁只堆陈设无表演区；**交通工具内舱（汽车/机舱/轿子/马车等）一般为独立主环境**（禁并进车外道路当衍生），按该载具类型惯例锁座向（汽车等同向朝车头、**司机在前排驾驶座**；轿子通常单人正坐、轿夫在外；马车先判车型——驿车常对座、敞篷/骡车常同向或侧坐），禁套厅堂围坐；**分位从简（强制）**——**四面（0/90/180/270 围合立面）允许加实体**（贴墙柜/挂画/靠边屏风等使四向可辨；门窗默认各一、不另开侧室、加件不侵表演中央）；**中间/舞台区按剧情需要**，表演净空中央只允许贴地层+硬核销表演锚；**靠边常用配置允许**（沙发、屏风、边柜及客厅/会客常用配套须靠边/贴墙，前方留主舞台净空，无该类型依据不加）；**活动区净空**——禁止把家具摆进主舞台/动线中央，其余陈设落四面边缘/背景向。**Stage 2.1 继承**：主环境行写 `activity_space:`、衍生行写 `activity_fit:`（摘抄上游，禁臆造；缺则 `upstream_missing_activity_space` 回流）。**中英同义去重（Stage 1/2.1）**：输入中英同义并列时只保留项目信息 `project_language`/`Language` 侧；专有名词与有动机异语除外。**原文逐字核销（Stage 1 最高）+入戏兜底**：原剧本每一处文字须落入优化稿可检索落点；对白/OS/可见字（项目语言侧）**逐字**保留（拆句可拼接还原全文）；叙述/动作/场景逐信息单元零丢失；专名逐字；**总闸——凡原文存在且尚未在对白/建置/ENV/角色设定/服化道/未落清单等他处完整落地的，一律写入对应 Beat【入戏】**（兜底优先于分类；不认识格式也不得静默丢弃）。建立空镜（`△俯拍/航拍`、`【空境/空镜：…】`）与景别行（`△近景/特写`、`【特写/近景：…】`）为常漏例，须入戏（ENV/场头不能替代；景别≠环境≠可删画面；特写沿用父 ENV）。禁概括吞并或删半句。Part1 须交【原文逐字核销】（含入戏兜底扫尾）；无落点=整场失败。与中英去重：只核销保留侧。**服化道/环境初始态锁定（Stage 1 §7 最高）**：服化道三项与【主环境】基准须**严格按原文初始建立描述**（场头/空境/开场/入场初态）；**禁止**把剧情发展后的狼藉、废墟、战损衣态、毁损道具等回填进初始摘要或主环境基准；过程变化进 Beat；跨拍可持续结构/氛围改写且**未**改写整体环境状况→状态衍生、**同场切 ENV**；**整体环境状况因受损/崩塌/火灾/域场改写且后段仍停留→新 Scene**（上场末拍=触发受损的前一刻、空间仍完好；下场从**实际发生的那个 Beat** 起写；触发与受损同拍须拆拍）；异地→按情节异地新建。Stage 2.1/3 基础版与主环境四向拼图继承初始态，剧变只落已声明衍生行；受损分场上场基准=完好、下场基准=改写后开场态。
- **方位坐标全链路同核（最高）**：Stage 1→2.1→资产设计一律**俯视鸟瞰顺时针**。以主环境 0°轴为原点：`0° →(+90°)→ 90° →(+90°)→ 180° →(+90°)→ 270° →(+90°)→ 0°`；下一正交角=`(N+90)%360`；对向=`(N+180)%360`；画左=`(N+270)%360`、画右=`(N+90)%360`。禁逆时针编号、禁第一人称左右推角、禁为好看对调 90/270。2×2 四向拼图排版（左上0/右上90/左下270/右下180）≠俯视平面图，世界扇区仍按顺时针度数锁。
- **主环境**：基准定义（俯视360+0度坐标轴；仰视360可选；**=原文初始建立态，非剧情终态**）；**0 度轴锚定优先级（唯一序）**：①上游已给基础/0度视角 → 继承；②否则 = 本场首个非航拍全局环境视角（Wide/Full/Master/Two Shot/LS 等眼高/地面级；**排除** Insert/CU/ECU/MCU/特写吸睛**与**航拍/高位俯瞰）的背景方位；③首 Beat 特写/航拍时尚无全局 Beat → 规划层可先以主路/主门/正堂/中轴线为 0° 背景半空间预声明并标「0度轴锚定 Beat」，待全局 Beat 锁定；**0 度方向 = 该视角的背景方位**（远端/背景半空间；规划 0° 扇区实体时优先挂主路/主门/正堂/中轴线）；**可不写 前景/中景/背景**，须写 **0/90/180/270 四向+中心**（**五区须可辨区分——四向=该向围合立面（允许加贴墙柜/挂画/窗沿陈设/靠边屏风使四向可辨，禁把四面件写入表演中央或侵入动线）、中心=表演净空或靠边常用配置/剧情硬核销地面家具（客厅沙发允许但须靠墙缘、前方留净空；禁把沙发屏风摆进走位中央；禁把中心家具记入某一向立面）；分位从简：四面允许加围合实体，中间按剧情需要，靠边常用配置允许；不论该区当前有无剧情：有戏表演中央须配合剧情且无多余件，四面须可辨不空洞；各向实体原则上不重复或不能完全同构，禁止四面抄同一套**；有需要另写**地面**与**空中/屋顶（或天花/天空）**；**上下楼层先判是否须共享外形**：通高可互望→一块主环境并分别写清下层与上层具名实体（禁「上层继承下层/二楼同楼下」）；**二层内部等不共享外形→切成两块主环境，各自独立描述**；可 `风格依赖`/`StyleReference` 让其中一块挂另一块主环境（只借年代材质色系工艺，不借四向/清单/0°轴）；禁把客房/账房并进一层当「上层」；**四向包络 = 剧情与环境描述的法定边界，不得向外扩张**；**四向正面边界锚定（最高；输入内容=正面面对；四方基准；禁偏斜）**：上游写在某向的内容即该向镜头所正对、正面面对的背景主景；主环境与衍生一律以四方（0/90/180/270正交）为设计基准，立面横平竖直；禁45°/墙角/荷兰角/菱形斜透视等偏斜构图；各向原则上正对四周边界立面正面——正对门/窗/主家具立面或端面/街口巷口廊口等，禁无剧情硬核销斜置对角斜切；例外仅原文斜向几何或开阔无立面时改以道路/地标轴线正对；Stage 3 四向拼图与衍生截取继承本条；**严禁对向串写**——0° 不许描述 180° 内容，90° 不许描述 270° 内容，反之亦然；对向实体只写在对向自己的段落），头尾双锚、固定实体清单（**实体唯一性优先**：原则上禁止物体重复，门/窗默认全场唯一，除非剧情明文需要；确须同类多件时先写个数→逐具具名+区分特征，并写清**分边个数/朝向/同侧顺序**（禁笼统「有椅子」）；**实体配套性**：功能成套须成套写入，如桌↔椅、床↔被褥/枕、杯↔盏/托；**含本场角色出入画所需具名出入口**）；Stage 3 输出**四向拼图**（16:9，2×2，眼高机位，非俯拍；**折中两段式**：【四面】每向后景+中景 →【四向拼图】引用二者 + 查表左右缘钉）；`referenceable:No`；**是未改写氛围下该主环境族全部视角衍生的默认生图参考源**；若已有仍在延续的状态/氛围衍生，则该状态行为当前空镜基准。
- **情节异地新建环境 + 闪回 Scene 分档（Stage 1 §7/§11）**：情节确认**不在当前主环境**时须新建【主环境】+对应衍生族（原文具名或剧情可核销异地意图均可；优先同环境复用）。**进入汽车/机舱/轿子/马车等载具内舱**须独立主环境（禁把舱内并进车外道路当衍生）。**出入封闭环境（强制）**：当场含出入过程时**通常以仓外为起始主环境**，入内后**本场内切换到仓内**（`入舱切主环境|出舱切主环境|回切仓外主环境`，禁标反打）；一般不另起 Scene。整场开场即在仓内、无出入过程 → 仓内可独主。**快速闪回/蒙太奇**：不新起 Scene，同场多主环境（每落点各一主+`0度`）；Beat 触发=`闪回切主环境|蒙太奇切主环境|回切当下主环境`（禁标反打）。**较长回忆片段**：须新起独立闪回 Scene（自有主环境族）。禁用当下衍生冒充异地；禁长回忆硬塞同场、禁快闪/蒙太奇误拆多 Scene。**闪回禁臆想多久前**：原文未写则禁止自拟「三年前/童年/N年前」等字幕或场注；叙事线只用`闪回`等闭集；时间感用**转场光学+运镜技巧**表达（**优先胶片质感升起/褪去**，可叠色调/叠化等光学≥1，且进入/回切各有可拍运镜过程≥1——如推近眼部、Whip Pan、Match Cut、慢推慢拉、Vertigo 等；**禁硬切入出**、禁硬切变色冒充），不靠臆造时长标注。分镜落地见 `shot_generation.md` §三.6 / §5.5 J。Stage 2.1 逐块提取；Stage 3 各主各自四向拼图、衍生只挂所属主。
- **未落环境实体清单（Stage 1 §11 强制，紧接【主环境】后）**：本 Scene 全场明文提到、且须在场内可挂靠/交互核销的实体盘点（道具/静物/可交互物件/载具等）；只列具名+须建置依据，不写七要素落位；角色/群演归【Scene实体覆盖】。与开场预排布交叉核销；清单外禁止中途凭空新增。Beat【建置】【入戏】对其中陈设级固定实体**只点名引用、不写其画面位**（多件才特指哪一件；见 Stage 1 §1 空镜重述）。多主环境时可标注归属主环境。Stage 2.1 可作 PROP 线索，**禁止**据此另建 ENV 行。
- **场头锁定必要道具与必要活动空间（强制；Stage 1 §11 继承并强化上游主笔 1i）**：写本场第一个 Beat 前须规划必要活动空间四项 + 必要道具/陈设（本场硬核销具名物入固定清单/未落清单）+ 空间位置关系（门/窗/主家具/出入口/活动区相对与四向归属一致）。**必要=剧本明确写出，或本场剧情硬核销必须用到**。**分位从简**：**四面允许加围合陈设**；**中间/舞台区按剧情需要**（表演中央只允许贴地层+硬核销表演锚）；**靠边常用配置允许**（沙发/屏风等靠边不占走位中央）；禁止往表演中央无意义加桌椅、扩出本场用不到的侧室。锁定后本场内环境骨架与空间位置关系不得无因改写——禁中途把门/桌对调或换向、禁拍中凭空多门多桌。剧情打碎/挪动/携入须有可拍过程且只改该物；未受力实体相对保持场头锁定。上游已锁 → 原样继承分位从简与锁定，禁优化阶段把靠边常用件搬进表演中央。分镜禁改写已锁相对、禁自拟把沙发屏风摆进走位中央。
- **建置剧情空间分析（Stage 2.1 / Stage 3 ENV·PROP；最高）**：资产提取与资产设计须通读 Stage 1【活动空间】/【角色与未落实体位置规划】/Beat【建置】【入戏】，分析环境与道具的剧情使用（对峙/逃窜/入座/上床/取物/出入画/桌面操作），把后续发展所需净空、动线、座次槽位、床头/床尾可达、桌面操作区写入 ENV `activity_space`/`activity_fit`/`purpose`/`fixed_*` 与 Stage 3 空镜几何（PROP 写功能+落点槽位+可读面）。**禁止**把角色名/人称/谁坐谁逃写入 ENV 任何叙述字段或 ENV/PROP `generation_prompt_cn`（空镜逻辑）；建置人名只作内部理解，入库/成稿前剥名。宿主仅机读 `host_character`。家具堵死活动区→回流 Stage 1，禁加装饰挤占剧情空间；**舞台区无清单项则保持净空，禁补件**。
- **角色与未落实体位置规划（强制；Stage 1 §11；写第一个 Beat 前）**：成稿块=`【角色与未落实体位置规划】`。须明文规划本场全部具名角色（含群演簇）与【未落环境实体清单】各项的**初态｜变化（Beat号）｜终态**。**规则只作内部分析，成稿禁止复述定位XOR说明/排位公式/自检清单**——该块只交结合本场剧情的站位分析结果。剧本出现`桌旁|凳子上|椅上|床上|柜台后|靠桌|入座`等 → 相对该实体写槽位；无挂靠才用画面前后左右；单主体禁混写。朝向、距离、正背面仍走原规则。须**逐人**写清与相关角色的前/后、左/右、尺度距离；内部排位①地位→②剧情→③重要者C位，成稿只写结论（`C位={名}（依据=…）`）。走相对者全场主锚尽量同一或两个。有方向锚须写桌边位/第几座/床头床尾+近远侧。Beat 1 建置≡开场初态；入戏 ⊆ 变化链；末拍终态≡规划终态。分镜禁改写规划落点、锚、槽位、C位与终点。
- **上游全局实体→场级场景描述明文（Stage 1 §17 最高；与 2.1 对齐）**：上游明文全局/贯穿/跨集/全剧级的**角色、道具、场景（环境）**，凡本 Scene 剧情涉及 → 必须在场级块具名明文（角色→【实体覆盖】+必要时【角色设定】；道具→未落清单+服化道「道具细节」点「全局性道具」；环境→【场景名称】+【主环境】与上游注册名逐字一致）。禁只埋 Beat 散文或只靠项目台账隐式继承。Stage 2.1 场级已标全局道具→强制提 PROP；仅项目明文而场级漏标仍强制提并回流 `stage1_missing_global_prop_scene_mark`。
- **实体唯一性（最高硬约束，Stage 1 §11⑤ / Stage 3；防生图识别混淆）**：各向实体描述时**原则上禁止物体重复**——默认每种功能实体**在所属主环境内**仅 1 具；**门/窗尤严，通常只有一扇**；除非剧情明确需要多门多窗/成套多件，禁止为填满四向而复制同名同功能实体。确须多件时才走「先写总个数→逐具具名+区分特征」。**通高两层**：一层件≠二层件，须分列具名并标所属楼层，禁「上层继承下层」。**同质多件排布（Stage 1 §11⑤-b / Stage 3；椅/凳等）**：≥2 具时须写清总个数、**桌各边/各向几张**、逐具朝向、同侧摆布顺序；**禁止**笼统「有椅子」。Stage 3 第一刀衍生 `generation_prompt_cn` §B 为只切割句（点名所属主环境四向拼图+宫格；成稿禁止保留四宫格分割线/格标/边框/拼缝；禁止写正对边界/四方正交/景深/开篇拓扑/楼底/坡向）；`visual_dependencies` 必须且仅能 `ENV:[所属主环境名]`（四向拼图）。衍生的衍生挂**同角已切割**衍生（`ENV:[{N}度{主}]` 或该角上一状态；N 必须与本行一致；禁挂他角、禁在已有同角切割时回挂四向拼图）。**全局楼底只在主环境开篇拓扑写一次**；各 `{N}度` 第一刀只截取对应宫格。Stage 3 美术增补**禁止**擅自把上游单门/单窗扩成多门/多窗。**四向拼图门/窗/楼梯唯一落位**：完整门扇/贴墙整跑梯只写在所属扇区那一格的**背景正对**（贴墙梯多为完整侧立面，按所属格×U；禁降级为仅中景斜升或仅左右缘窄条——否则无基准、各格不一致）；90°/270° 禁止再写「大门/玄关大门/楼梯」作第二件。**条形实体统一（Stage 3）**：中区长桌与边上贴墙柜台/楼梯同一套开篇「长轴+两端具名锚」与成稿「纵深/横跨/贴边短端面」；邻向窄条必须点明是哪一端，禁无端锚自由侧切。**楼梯有朝向**：须写上行朝向 U（走上时面朝的世界度数，≠所在坐标）；U≡F 查可见面；**坡向挂在横跨句上用画面角点锁定**（`N=U+270`→左下→右上；`N=U+90`→右下→左上），禁笼统「侧面格左低右高」，禁镜像；**侧面格严禁写成正面迎面层层拾级**。**歧义控制**：一名一物、一物一位、坐标≠朝向、定位词≠搬件。
- **固定实体数量闭合（最高；Stage 1→2.1→3）**：禁止 `若干|数把|多张|一些|成排|散座` 等模糊数量词；同质多件须写总数、分组/分桌/分边、逐具 F 与同侧顺序，并验算`总数=分项和`。门/窗/楼梯总数显式；每扇窗棂须写所属扇区、开闭态、横×纵格阵及同墙顺序；楼梯须写 M/U/下口/上口/基准格/坡向。Stage 2.1 缺项须标 `upstream_missing_env_inventory_quantification` 回流；Stage 3 严禁猜数或以“若干”兜底，但 `generation_prompt_cn` 不得留空（骨架+已锁具名实体；缺口只进 logic）；图片无法可靠计数时保留 Index 数量锁并标 `count_unverifiable_from_image`。
- **实体配套性（强制，Stage 1 §11⑤-c / Stage 3）**：功能上成套/依附使用的实体须成套写入固定实体清单——典型 **桌↔椅/凳、床↔被褥/枕、杯↔盏/碟/托、沙发↔茶几/矮几**（及笔砚、灯座等同类）；禁止只写主件漏配件；例外仅限剧情明文残缺/空置/异形制。**配套只补已入清单主件的配件**；禁止为凑配套而新增主件（无桌不加椅，无沙发不加茶几）。Stage 3 须完整继承配套关系，禁只画主件或擅自拆套；四向拼图每格主件可见则配套几/椅须同格交代（全挡须显式遮挡）。
- **中区地面固定家具跨格守恒（最高；Stage 1 五区【中心】/ Stage 3 四向拼图）**：茶几/矮几/中央桌/岛台/沙发前方配套几等**不贴单墙的中区家具只属【中心】**，禁止记入 0/90/180/270 某一向立面主锚。邻向入境与对向串写**只**约束贴墙立面（门/窗/挂画/贴墙柜/廊口）。Stage 3 四向拼图：各格**背景=该向围合立面**，沙发与矮几进「中景-中央家具」；凡几何可见必须在该格具名。沙发长轴 90°↔270°、正面朝 180°、矮几在沙发与大门间时——0° 矮几在沙发前方完整可见；90° 矮几在沙发朝玄关一侧（画面右）完整中景；270° 矮几在沙发朝玄关一侧（画面左）完整中景；180° 在沙发远侧，全挡则显式「被沙发背遮挡」。禁止 0° 写了矮几、90/270 蒸发；禁止把矮几当 0° 邻向立面缩成贴边斜切。**完整防盗大门只在 180° 格背景**；90/270 禁止再写「玄关大门」导致重复开门。
- **招牌灯/牌匾可见文字（Stage 2.1/3 强制）**：牌匾、匾额、店招、招牌灯、霓虹、灯箱等画内可读字须写清 **排向=`横排`｜`纵排`**、**字体/书体（含字色、底色/匾色；招牌灯/霓虹/灯箱另写发光色）**、逐字文案、繁简体。禁只写「牌上有字」而无排向或字色。Index 可检索 `visible_text`+`text_layout`+`typography_requirement`+`script_variant`。**四向拼图广角例外**：中远景柜面/桌面小物件（账本等）Index 仍存四项，但 `generation_prompt_cn` 只写轮廓「字迹不可辨」——禁逐字塞广角生图（否则易另造前景柜面）；逐字留给特写/PROP。
- **四向邻缘（Stage 3）**：画左=`(N+270)%360`、画右=`(N+90)%360`（扇区方向禁对调）。**180° 强制验算**：画左=90°、画右=270°；开篇楼梯在90/散座在270时，**禁止**「左散座右楼梯」或柜台左右端锚对调（一票否决）。**左右缘只挂交角最近件**。**0° 楼梯缘**：须 `可见面=侧边` + `禁止迎面` + `同一跑下口端切片｜forbid short stub`（缺一失败）。**180° 上口缘**：须 `单条连续｜forbid double-segment / second flight`。**条形两端命名跟长轴**（90↔270 禁写 0度端/180度端）；同轴柜台与方桌左右共核。**四格可见面必填**。**同向后景多实体**须写左右/上下相对。**左右缘抗升格**：`本格唯一背景主景=` + `贴边窄条｜绝对非背景｜peripheral fringe only; NOT the background`（不强制百分比）。
- **固定实体清单零遗漏（强制，Stage 1 §11⑥/Stage 2.1/Stage 3 三段共同责任）**：本 Scene **剧本明确提到或剧情硬核销必要**的全部实体（不论道具级/装饰级/背景级/一次性露出）均须写入主环境固定实体清单，不可因"次要/不够重要"省略；**分位从简闸**：表演中央未在剧本写出且非本场剧情必要者**禁止列入**（禁为填走位中央/场面丰富而造件）；**四面允许加围合陈设**；**靠边常用配置允许**（沙发/屏风等靠边，可列入固定清单）。**是否升格为独立 `PROP` 与 Stage 1 是否收录入主环境清单无关**——Stage 2.1 按 **道具提取极严原则** + `ENV/PROP` **XOR**：默认可归 ENV/CHAR、无状态变化、或仅一次性出画（视频模型可按 Beat 处理）者**不提 PROP**；**例外——剧本明文全局性道具（全局/贯穿/跨集/全剧核心道具等）必须提 `PROP`**（覆盖极严四禁）；其余仅不可归 ENV/CHAR 且有可持续关键状态（或跨场复用资产）者升格为 `PROP` 并从 ENV 剥离；未升格者仍须保留在 `ENV` 固定陈设或 CHAR `clothing:`/`appearance:`；**禁止**同一实体既在环境/角色描述中又被提取为道具。Stage 3 描述时同样须逐一转译，禁止在环境与道具两侧同时消失；四面与靠边常用件可按分位补，禁止转译时往表演中央加无清单实体。
- **空间均衡性 / 活动区净空（强制，Stage 1 §11⑦ / Stage 3 §2.2）**：固定实体布局与美术增补须防过挤（主舞台/动线阻塞）；**分位从简**：**四面允许加围合陈设**使四向可辨；**靠边常用配置允许**（沙发/屏风等靠边）；**表演中央过空≠往走位区加家具**——无剧情锚时中央偏空=正确默认。次要陈设落于四面边缘/背景向；增补偏空优先在四面与墙边增。**活动区禁止在表演中央落立体障碍**：【活动空间】主舞台/动线中央禁止堆家具；仅允许地毯等贴地实体、剧情硬核销表演锚，或**靠边常用配置**（沙发靠墙、屏风靠侧，前方留净空）；禁为「不空/好看」把沙发屏风搬进对峙/打斗中央。
- **角色四向分布（Stage 1 §12 原则）**：无剧情站位冲突时，画内角色可按主环境 **0°/90°/180°/270°** 分散；**逻辑可拍 / 礼仪尊卑 / 剧情站位 均优先于「均匀填角」**——禁为凑四向把人塞进不可拍位或破坏对白轴线。角色间前后左右距离须逐人明确；排位①地位→②剧情→③重要者C位。**人群分析与四向分组**见下「单 Beat 3 人以上分组」。
- **角色环境速查（Stage 1 §12 强制；并入【观察视角与空间建置】）**：目标=锁定每角色/组主场衍生 ENV + **世界扇区** + **F** + 姿态/锚。Scene 级速查表 + 每拍末`速查更新`（=终态）为跨拍真源。落笔序：**正背面档位开拍检→当前 ENV↔背景半空间匹配→世界态→先算`后景=N/前景=(N+180)%360`再查表→【建置】【入戏】**；切角必查换算表。**N=180 时 0°侧=前景、180°侧=后景**（禁把 0° 当永远后景）。F 映射：F≈N→背对/侧背面；F≈N+180→正面（禁侧背面）；F≈N+90→右侧面+面向画右；F≈N+270→左侧面+面向画左。建置禁 `F≈`；速查扇区查表须≡建置层位≡台账（有走位则入戏链闭合）；算术与建置反号禁标「①–⑧全过」。≥2 人同层须分画左/中央/画右。`ENV变=是` 填`视角换算台账`（含算术头）；反打禁换主环境名。**单拍单正背面档位**（建置≡入戏≡终态；禁同拍正+背）；**转身/转面致档位变→强制另起 Beat**；**出入画致开拍集≠收拍集→过程拍落定后强制另起 Beat**（后续对白/主动作不得同拍续堆；切角暂不可见≠出入画）；**切镜头致档位变→另起拍且切 ENV**。**建置防错乱五锁**：时序｜投影｜朝向｜ENV名｜集合（新人禁凭空入画）。
- **当前 ENV↔背景环境匹配（最高；衍生行唯一约束）**：本拍当前环境永远只能是完整名 `{N}度…` 衍生行（Master=`0度{主}`，禁裸主环境）。按背景内容选角时，唯一直接依据是候选衍生行自身的`活动适配+可见内容白名单+可见边界`；所属主环境只校验角度/拓扑/资产归属，禁止把主环境四向描述或固定实体总表套入当前镜头。静止拍亦须逐项核销白名单；实体未列入当前衍生行即不可见，必须呈现则先补建/切换衍生 ENV。禁机械轮角、无据粘0度或用主环境本体代替0度。
- **当前衍生 ENV 可见内容总闸（最高；严禁套主环境）**：每拍可见内容的**唯一直接真源**是当前完整名 `{N}度…` 衍生环境行/资产自身的`活动适配+可见内容白名单+可见面/可见边界`；所属主环境只作资产登记、世界拓扑与一致性校验，**不得**以主环境四向描述、固定实体总表或全景提示词补当前镜头。主环境有某实体≠本衍生可见；未命中本衍生白名单即禁写。角色/可移动主体另按N投影。每处画面事实逐实体判`V可见|D画外方向/声源/作用来源|X不可见`；D只写方向/声音/作用在画内对象上的结果，X只进差集。必须呈现D/X本体时，先另起Beat并切到白名单覆盖该内容的已声明衍生ENV；缺可读角则先补建衍生行，禁直接套主环境或用Push In/CU/角色望向/旁白绕过。自检必填`约束来源=当前衍生ENV`、`主环境直取内容数=0`、`不可见内容描述数=0`。
- **衍生轻量白名单字段（与跨阶段职责边界并行）**：Stage 1 每个 `{N}度…` 衍生行除角度元数据外必须含`活动适配`、`可见内容白名单={具名实体:可见面}`、`可见边界`、`不可见内容`；这些仅是当前镜头可见性许可，不是四向美术/前中后景空镜设计，后者仍只由 Stage 3 执行。白名单候选来自所属主环境登记，但 Beat/分镜不得反向从主环境补白名单外内容。
- **主环境180°对向位零可见（最高；固定环境实体）**：当前衍生观察轴为N时，主环境 `(N+180)%360` 边界上的固定实体及其附着/承托物全部在机位后方；必须全列入该衍生`不可见内容`，且`可见内容白名单∩对向位=∅`。禁以背面/侧面、前景、广角或稍带到为由写入。N=0时180°柜台/酒架及其算盘账册等不可见，`0度大门`禁列`柜台:背面`；N=180时0°大门及门外不可见。相邻±90°仅可按可见边界贴边小部分入镜。此规则针对环境边界固定实体；可移动主体仍按N投影表判定，不因位于N+180世界扇区自动消失。
- **Beat剧情需求驱动衍生ENV设计（最高）**：先做`Beat→衍生ENV剧情覆盖矩阵`，逐拍列角色世界锚/承托面、交互固定实体、道具及须读面、环境特写、出入口/终点、特效命中、背景叙事锚，再要求`全部必须可见项⊆当前衍生白名单`。角色写`柜台后/桌旁/椅上`时该锚及交互面必须同显；`拿起桌面酒盏`须执行者+桌面+具体酒盏可见面同显，`桌区`集合词不能隐式授权酒盏；环境特写同锁。若一拍必见项跨N与N+180，对向零可见优先，必须拆Beat并在描述前切ENV，或新增合法侧向衍生，禁用全画幅/广角套主环境。自检要求`挂靠不可见=0|交互链缺项=0|环境特写未授权=0|单Beat跨对向=0`。
- **衍生ENV选角集合算法（最高；禁默认0度/Master）**：先区分`必须可见R`与`仅画外D`（声音/对白提到/画外来源不强制本体可见），再把`柜台后站位→柜台交互面`、`压算盘→算盘顶面`、`从账册移视→账册可见面`、`酒盏水面特写→桌面+酒盏顶面/内壁+水面`标准化展开。对每个候选衍生列白名单W并真实计算`R−W`；只有差集=∅才合格，禁口头标“子集=是”。按`完整覆盖>交互可读>角色关系/露脸可拍>守轴>切换成本`选角，0度无优先权。现有角不覆盖时优先新增合法侧向/剧情覆盖衍生；物理上单角无法覆盖才拆Beat切ENV。`可见边界=全画幅|整个大堂|全部可见`无效，须具名四边/近端截断。
- **衍生环境设计：内容驱动 + 禁无动机越轴（Stage 1 §11）**：先按本场 Beat 实际需要的背景内容建角，禁机械凑四向。定 `spatial_axis` 后守轴——反打/OTS=`PlannedReverse`（两步结论）；仅剧情明文动机才可 `MotivatedCross`；禁止无动机跨轴乱跳度数当「换角」。
- **上集/既有主环境注入→衍生角度锁（Stage 1 最高；有注入则强制）**：输入含 `[已存在的全局主环境约束]` 或上集/跨集须复用主环境时——主环境 0°轴与四向扇区同核延续；本场每一 `{N}度…` 衍生须与该既有主环境**同角完全一致**（禁重映射度数/旋转坐标系）；Beat 选可拍衍生**只**按该环境四向设置与 `活动适配`（后景应见何物）匹配，**禁止**随机选角或按 0→90→180→270 顺序轮角。Stage 2.1 透传 `view_angle_from_main`；Stage 3 衍生截取同角扇区。
- **环境固定实体只点名、不写其位（Stage 1 §1 空镜重述；建置+入戏）**：桌/椅/床/门/窗等【主环境】固定清单或未落清单陈设——Beat【建置】【入戏】**禁止**复述其前景/中景/后景、画左/中央/画右等画面位；**直接引用实体名**（`坐在椅上`｜`靠桌`｜`经大门出画外`）。同类多件须特指哪一件时才用具名区分特征消歧，仍禁借机重写该实体层位。可移动主体按**定位 XOR**写自己的位置（无剧情联系→视角绝对；有联系→相对主参考实体；单主体禁混写）；挂靠陈设时只写相对**名+槽位**的关系，**禁止**再叠前景/后景/画左。终态与建置同方式：相对行禁前中后景+画左，绝对行禁相对锚。
- **前中后景及左中右相对参照公约（最高硬约束）**：角色/道具**定位**服从 XOR（无剧情联系用本条镜头绝对坐标系；有剧情联系用主参考实体相对定位，禁与本条绝对层位句混写）。朝向、距离仍本条。凡写方位，一律以镜头朝向看——并在成稿必须明确标示距离，如前景(离镜近处)=离镜头最近，背景(远处)=最远，并区分画面的左边和右边。【建置】【入戏】除环境名外【严格禁止】出现面向0度/背向180度/0°侧/躯干面向N°侧等世界度数推演；度数世界态**只**进每拍`速查更新`与 ENV 变时`视角换算台账`。**本条方位粒度针对可移动主体**——环境固定陈设不写其位（见上条）。绝对禁止只写“中景”或“前景”而不带说明括注！躯干朝向等必须转换为“背对镜头、画面左侧、背景远处、前景(离镜近处)、面向画右、面向{具名对象}”等镜头相对词；**换镜后必须按新 N 重投影，禁粘贴上拍画面词**。禁止以世界前墙/后墙定义层位。**朝向≠人所在层位**；**扇区≠景深**；机位后方不可见禁写背景。**原则栈**：①**逻辑合理可拍** → ②原文核销 → ③硬锁（投影/三联/四元）→ ④模板配对（可改）；公式不得倒逼物理不可能句。**世界态为投影真源（手段）**：真源=`扇区+F+姿态+世界锚+随身件挂载`（跨拍继承）；画面词=投影。`ENV变=是`且无走位时须按新N逐项重算；**不是层位/左右/正背面三项强制全变**，轴心或数学恒等项可不变但台账须标`投影恒等`；未动者世界锚/姿态/挂载/F不得因切角改变。**朝向三联·面向层位词≡对象实层（最高；Stage 1 §12；对象含人与环境锚；≠入画径向三联）**：`面向前景/后景/中景(的){名|龙|天穹|裂口|门|锚}` 中层位词必须≡该对象本拍实层（有无「的」同检）；写面向句前须先列同拍实层表再核对；禁后景人写作「前景」以凑公式；**禁同层互称前/后景**——同层只用画左/画右或先以可拍走位拆纵深差≥1档；**“同层偏近/偏镜头侧”不是合法档位**。**绝对禁**：既`正面/¾朝镜头可读`又`面向后景…`（零例外）。正面朝镜头⇒若写面向他者，对象必须明确处于不同纵深且比己更近；对象不更近→省略或写镜头向。背对⇒对象更远。**逻辑合理可拍（最高总闸；废除正面优先）**→硬锁服务于可拍。正背面只由面向世界目标→F→`f(N,F)`决定；仅剧情须读脸/口型/正面躯干时才安排正面/¾。正反打戏份可拍配对：近侧背对面向后景对手 ↔ 远侧正面面向前景对手（同向戏份禁硬套）。**露脸对象优先+开场冲突预评**：先定谁须露脸，再安排他人；需改朝向用专用转身过程拍，换可读方用反打/OTS ENV。**角色位置关系前瞻**：正反打/OTS→不同纵深层且相差≥1档；同向并肩→同层分左右。**动作空间与角色距离**：场级评估动作净空与角色间距；动作起止重评。**停驻落点与他者相对**：运动停下交代定位（XOR）+尺度距离+相对关系。**建置实层表（强制首行）**：有纵深落位或层位面向词时先写实层表；层位词只许抄表。ENV变/回挂靠后须重核三联、世界锚、姿态及随身件。
- **Beat开拍评估（强制，Stage 1 §12；写入切换说明）**：每个 Beat 落笔前须评估——①本拍**运动合理性**（净空/步幅/终点/门径向/不穿障）；②当前衍生 ENV 的**四周与舞台中间**内容与**角色建置**是否匹配（后景主锚、舞台净空落点、挂靠实体名属本角可见半空间）。结论写入【Beat切换说明】`Beat开拍评估=`；失败须回改，禁空标通过。
- **建置初始态 / 切换说明终态（强制，Stage 1 §1/§14/§15）**：【建置】=画面**开拍初始态**（开拍可见集）；【入戏】=运动过程；【Beat切换说明】=**开拍评估结论** + **初始建置 ⊕ 入戏运动**的变化过程 + **`终态画面纵深`**（收拍可见集，与建置同粒度），供下一 Beat【建置】承接（同 ENV 抄终态；ENV 变承`速查更新`再重投影）。**纵深双落按切片**：开拍在画→建置+终态均有纵深；中途入画/突现→终态必有（建置可不含）；中途出画→建置有、终态无。禁把运动后落点写进建置；禁切换说明省略终态纵深或只写「通过」；禁无条件「终态≡建置」。**出入画改集合另起拍（最高）**：明文入画/出画致开拍可见集≠收拍可见集时，该拍只承载出入画过程（入画者当拍写满终态字段）；落定后**强制新起 Beat**以新集合为【建置】承接后续对白/主动作——**禁止**同拍出入画落定后继续堆对白轮次或新主动作（切角暂不可见≠出入画）。
- **不可见实体分区（最高；Stage 1）**：查表机位后方/画外/完全遮挡的**不可见/暂不可见**实体——**禁止**写入【建置】（亦禁「不可见的{名}…」工程句冒充建置）；**必须**写入【Beat切换说明】的`差集处置`（全字段续挂）+`速查更新`（`可见性=暂不可见`）及`ENV变`时`视角换算台账`；禁从建置删除后切换说明蒸发。转可见后才入建置。
- **入画径向三联（最高；Stage 1 §12；≠朝向三联）**：经门入画须同时闭合——门层位｜行进径向与正背面｜当前 ENV `活动适配` 可见面。❌`0度`(可见大门)+「前景背对镜头、推门向内」——背对向内应挂「可见王座/内侧」的 `180度…`，门放近端；若挂可见大门的 `0度…`，则须自背景门朝镜头入、终态正面。❌外景航拍挂厅内「可见大门」ENV。**入画径向↔正背面硬锁**：背景门入 → 朝镜头逼近 → 终态正面/¾、门在身后；要背影进深处 → 门须近端/画外 **且** 切至可见内侧的衍生 ENV；禁同 ENV「背景门 + 前景背对 + 面向深处」。
- **Beat 完全独立自洽与建置内容完整继承（最高硬约束）**：即使当前Beat的机位、人物站位和上一Beat【完全相同】，也【绝对禁止】在建置段使用“同上”、“承接上个Beat”、“ENV不变”之类省略。必须把上一Beat终态（结束态）所有可移动主体（角色/动物/载具/法相等）的方位、朝向、姿态【一字不落】地直接复制书写到当前Beat的建置中（作为本Beat基础）！每个Beat抽出来都必须自带完备的视觉定位，禁省略定位：绝对方式须写左中右+远近；相对方式须写主锚槽位+近远侧+边位；禁绝对+相对混写。
- **Scene 不适配须拆 / 场景描述融入 Beat（Stage 1 §9）**：输入场次若含明显不适合当前主环境的动作/对白（真正越出四向包络/异地异线），须另拆 Scene（禁硬塞本场）；**同场故事不分场（对齐上游 1f）**：同一主空间骨架内的连贯戏（对白改打斗、换观察侧、反打、走到另一侧）**禁止**另起 Scene，一律本场内切换衍生 ENV。闪回分档见§7——**快速闪回/蒙太奇**同场多主不新起 Scene；**较长回忆**须新起闪回 Scene。**整体环境状况因受损改写**须新 Scene 时，切口=上场末触发前一刻、下场首拍=实际发生。原文场景文学/氛围描述、`△俯拍：…`、`【空境/空镜：…】` 建立句，以及原文`△近景/特写/…`、`【特写/近景：…】`景别行，均须融入 Beat 入戏（优先独立首拍/吸睛/特写切口拍；无人戏亦可；航拍不作 0°轴；特写沿用父 ENV），禁只留场头或【主环境】文学说明，禁因「景别≠环境」丢特写画面。
- **单 Beat 3 人以上分组（Stage 1 §12）**：单 Beat/分镜同框超过三人须分组（每组≤3），各组分布到不同环境角度（**优先 90° 倍数**衍生 ENV），分 Beat 分批建置；禁止单拍硬塞全场。**环境物理隔离**：角色较多时分组后须在主环境布局上适当隔离——同场入座/落位超过四人优先两桌/两区（或等价分席），禁无依据超四人单桌硬塞。**群演延迟入画**：群演/龙套/无具体剧情功能者尽量勿开场即入画，待合适 Beat 用动作经具名出入口入画。**一旦入画后守恒（强制）**：已入画群演/角色须后 Beat 逐拍承接——可见则入建置，暂不可见则差集续挂（挂靠视角+相对锚），或明文出画；**禁止**无交代从建置/`{Beat终态画内实体}`/差集池静默抹除——群演不豁免；切回原视角或他角使可见时必须恢复带入。**群演随视角建置变化（强制）**：`ENV变=是`/切角时，群演簇/龙套/手下须按新观察轴**重判**——仍可见则重写画面句并入台账，暂不可见则差集标注，禁止粘贴上一拍画面句或因「只是龙套」忽略；同 ENV 未切角且仍可见时才可原样抄写。**群演分组与主人物空间距离（强制）**：群演按分组原则自成气氛/背景组，**尽量**与主要人物拉开空间距离（不同扇区/更远层位/边缘区）；禁止无交互依据贴肩贴桌抢主舞台；近身仅限剧情明文交互。
- **可移动主体建置含进出场 + 后 Beat 守恒（Stage 1 §1/§15 最高硬约束）**：建置须把进出场纳入闭环；主体**不仅**人物——凡有移动可能的具名实体（角色/群演簇/具名手下/**动物/宠物/坐骑**/**车辆等载具**/**法相/金身**及人态化巨兽等）已在场后 Beat **三态**——①查表可见（入建置+终态）｜②明文出画（经具名出入口/驶离飞离/消形）｜③暂不可见（**仅**查表=机位后方/画外，≠出画：**禁入【建置】**；**必须**`差集处置`全字段+`速查更新`可见性=暂不可见）。**上拍差集处置须本拍重析**；**当前ENV=挂靠视角 → 必须已转可见并带入建置**（禁用「景别收紧」挡恢复）；查表得前/中/后景亦必须带入。切角/ENV切换≠出画。**名单闭合（开拍集≠收拍集）**：【建置】≡开拍可见集；`{Beat终态画内实体}`≡收拍可见集；**`终态=(建置−本拍出画)∪本拍入画/突现落定`**——禁无条件「终态≡建置」；中途入画/金身凝形者建置可不含、终态必须含且≡角色全字段；**开拍集≠收拍集（出入画）→过程拍结束后强制另起 Beat**（见上「出入画改集合另起拍」）。`ENV变` 台账须含输入池全员（含随从/动物/载具/法相/金身）。**入戏方位硬锁**：`对画左/画右的{名}` 须与建置落位左右一致；对象居中则禁写画左/画右。
- **身体姿态必填 + 跨拍严格继承（Stage 1 §1/§12 最高硬约束，全场景）**：可移动主体建置须逐具明文写清当前姿态/停驻态——站/坐/躺/跪/蹲/趴/倚靠/骑乘/停靠/行驶中等；禁止只写位置/朝向而省略姿态；座/躺/倚须挂具名承托面。**承接上拍时外置（扇区/锚）与姿势档一并严格继承**；切角/反打只重投影层位与正背面，**不得**无过渡把跪改站、坐改立；姿态变更须本拍入戏可拍过渡，建置写旧姿态、速查写新姿态。
- **可移动主体建置等同角色（Stage 1 §1/§8/§12 最高硬约束）**：【建置】覆盖全部有移动可能的具名实体——角色、**具名龙套**、**群演簇**、动物/宠物/坐骑、车辆等载具、**法相/金身/法身/真身/金光法身/万丈金身**/化龙/化凤/巨兽等——须按角色同等建置。**群演／龙套建置零豁免**：全部建置硬约束（五锁/朝向四件套/正背面/姿态/出入场三态/不可见分区/切角重投影等）对群演簇与龙套**同等生效**——龙套≡角色全字段；群演簇须簇级**人数/规模+位置排列/队形**（军警/军队等须写整齐纵队/方阵等可拍行列）+定位 XOR（绝对=纵深+左右；相对=主锚槽位+近远侧）+主导正背面/面向+姿态；禁因「气氛/虚化/非对白焦点」省略；禁只写「有军队/众人」无规模与队形。**金身／法相建置≡角色（字段零减免）**：定位 XOR+正背面+面向+姿态+与他者+视线（有则）+动作方式——句式与角色同模；**禁止**只写「显形/撑满天地/右掌向前」而无位置与朝向；沈渊建置≠金身建置，同框须分名分写。**宿主↔法相／金身动作同步＋因果共写（最高）**：维持相期间**原则上与原主动作同步同相**（可巨化／极短延迟）；与宿主有直接因果（凝形/借体出手/同损/收摄）时，【入戏】须**同拍同时写清双方动作**并咬合因果链（宿主驱动 → 法相同向/放大对应可见结果）；禁只写一侧、禁无核销不同步连招；不同步仅失控／反噬等明文例外；分镜 Video 同相位继承。**打斗／法攻 OTS 正反覆盖（最高）**：打斗、斗法、投射法攻等对阵须参照对白 OTS／正反打——各方出手与受力／挡格均须有可读角与反打可读角；禁全程单侧只拍一方；决战每轮至少 1 次正↔反 ENV 切换；分镜用 OTS Switch／Reverse 落实。**正背面由 F/剧情决定（废除正面优先）**：本拍主出手/对白主拍/凝形主体（**尤金身/法相**）正背面走落笔序（目标→F→`f(N,F)`）；**仅**须读脸/口型/正面躯干时才安排正面/¾；禁无核销默认正面，亦禁无核销乱背对。**入画／突现当拍建置**：凝形/拔地而起当拍即写完整角色级建置句入终态/速查；入戏`金身：`行有名则建置或终态必须字段齐全。纯无定形介质（金纹/波前/光晕）不替代金身本体建置。**新入画定位特写（一般）**：具名角色/重要实体首次入画须特写或近景交代位置、朝向、姿态、表情（或可见面微表演）；背对入画须读脸则另起拍切可读角。
- **剧情随身物件建置守恒（Stage 1 §12 / Stage 2.1 / Stage 3；最高）**：角色配饰、包袋、武器及任何已出现/原文直接提到的有剧情随身件——须写清挂载/持握部位并服从全部建置要求；可视性与宿主正背面同锁。**无明文出画/放下/移交/收纳等离身链时，下一 Beat【建置】必须重复上一拍终态对该物件的描述**（禁默删、禁「同上」省略）。宿主暂不可见则随身件随差集续挂；回可见随宿主带回。Stage 2.1：**道具提取极严**——可归 CHAR 造型、无状态变化、仅一次性出画者不提 PROP（进 `clothing:`/`appearance:` 或留 Beat）；升格随身件必写 `wear_side`/`mount_body_part`/`host_character`/`detachable:否|是`；**仅不可离身**（`detachable:否`）宿主 CHAR 写 `accessory_props`+`accessory_mount`。Stage 3 角色：仅不可离身配饰挂 `visual_dependencies` **并**写入 `clothing`/`generation_prompt_cn`【衣着】挂位；可离身升格 PROP 不挂角色依赖、不进角色生图词。
- **发声屏设备位向（主笔 9d / Stage 1 §12 / Stage 2.1；最高）**：手机、iPad/平板、智能手表、开盖笔记本、对讲机、车载屏等能发声、有可见影响、分正反面的设备——凡写到须同时写清**现位**（哪只手上 / 哪张具名桌上 / 已收纳不可见）与**正面朝向**（屏面朝持有人/朝上/朝桌面/朝向在场人）；响铃/振动/外放声源挂该设备现位；屏面内容仅当正面朝向可读者才可写；背面朝上/朝镜禁写可读界面。无明文拿起/放下/翻面/收纳则逐拍重复上拍位向。缺位向→Stage 2.1 `upstream_missing_prop_orientation`。
- **取物先落位旁（主笔 9f / Stage 1 §12；最高）**：拿起/取出桌面、柜面、架上、地上等已放置物品前，角色须先站/坐/停在放置处旁边（一臂可及）；不在旁则先走近停驻再伸手。禁隔空取物（人在门口却拿远处桌上物）。已坐同桌且物在该桌面、或物已在自己手上/口袋除外；他人递交走递交链。
- **触达/操控须距离可达（主笔 9i / Stage 1 §12；最高）**：操控道具（拿/按/翻/开/关/拖）或触达其他角色（抓/推/拍/递到手上/捂/近身击打）须先据建置尺度距离判定是否物理可达（默认一臂；触人贴身～一臂）。不可达 → 先走近停驻再伸手，或改用远程手段。**远程可无视一臂（须明文）**：气/灵气/念力/力场、箭/暗器/投射、电子科技（遥控/App/对讲/电子锁/无人机等）——必须写清触达与操控方式 + 具体动作（气=可见形态+轨迹+作用；箭=出手→路径→命中；电子=点哪键/哪台设备/何反馈）。禁只写「用气拿剑」「远程开门」无过程。分镜禁把不可达拍成隔空伸手。
- **实体物件可视性核验（Stage 1/2.1/3；最高）**：描述任一实体物件细节（铭文/发光/破损/正反面/屏幕字等）前须核——画内｜朝向面朝镜｜挂载/持握与宿主正背面闭合｜无被挡——镜头不可见面不得写成可见事实；须读该面则另拍/翻面/切角。Stage 3 四视图各格只写该视角可读面（道具正背侧分面；角色侧：不可离身升格配饰与未升格纯装饰按挂载体侧落位；可离身升格 PROP 不进角色生图词；ENV 禁对向串写）。**ENV 资产生成（最高）**：实体**可见面**须写入**对应** `{N}度…` 衍生环境锚点（`anchor_description` / §A / 该宫格成稿）；**不可见面严禁写入**该衍生锚点或生图词（禁机位后方/对向面冒充可见）。
- **开场预排布静物/道具（Stage 1 §1/§12/§17 最高硬约束）**：建置规划须通读全场、以前瞻【未落环境实体清单】为盘点表，**不得只按本 Beat 戏份取舍**。无自主入画能力的静物/道具（桌面物、陈设级可交互件等）**默认开场即在场**——写入【主环境】固定清单与/或【未落清单】/实体覆盖排期，即便开场尚无其剧情交互；**禁止**等到有戏份的 Beat 才突然出现。Beat【建置】【入戏】**不**为其复述前景/画左等画面位，交互时**只点名引用**（桌/椅/床等）；仅同类多件须特指哪一件时才用具名区分特征消歧（仍禁借机重写该实体层位）。仅当清单/【Scene实体覆盖】明文「待入画」且有携入/投掷/容器开启暴露等可拍过程链时方可晚到。与「道具不得凭空消失」对称：先预排布（清单在场）、后承接、禁凭空进出。**边界**：动物/载具/法相等可移动主体走「可移动主体建置等同角色」，不按本条静物延迟；静物一旦本拍起位移/被携出画→升格为可移动主体并开进出场守恒。
- **并排/队列中间位必标（Stage 1 §1/§12）**：建置涉及并排、队列、一字排开、纵列、夹中等使 ≥3 具名角色成列/成排时，中间位具名人物须明文标槽——横向写左/中/右，纵向写前/中/后；禁止中间人只写「夹在甲乙之间」而无 `中`。
- **他人目光默认朝向说话人（Stage 1 §12 对白咬合）**：某角色画内发言时，同框其他应可见角色目光/视线默认落在该说话人；仅当剧情刻意表达冷漠、无视、回避、分心等时才可例外，且须明文写清例外情绪与实际视线落点。
- **对白单说话人闸（Stage 1 §12）**：一个 Beat 只允许一个具名说话人/声源主体；第二人插话、答话、喊话前强制另起 Beat 并重做建置/选 ENV。自检必填`说话人去重数=0|1`，≥2=失败。背对/侧背面可发声但写`发声（口型不可见）`，禁写`开口/唇线/嘴角`等不可见口型。
- **建置人物/动物入戏动作覆盖率100%（最高）**：每个Beat建置内可见人物、具名/非具名龙套、动物/坐骑，入戏须逐名至少1个可拍且符合剧情的动作、目光变化或微反应；`站立/坐着/在场/保持姿态`不计。非焦点只做不抢戏的低幅度动作，禁为过检新增走位/转身/取物/对白。正面可写表情，侧面只写可见半脸，背对只写肩背/后脑≤30°/手部/重心；动物分核body/head。群演簇可一条簇级微动，具名龙套仍逐名。原文明文死亡/昏迷/冻结等才可静止，且须写静止证据/被动反馈。自检必填`建置人物动物S`、`入戏动作覆盖A`、`S−A=∅`、`动作覆盖率=100%`。
- **剧情角色按剧情触发当拍入画（最高；氛围龙套/群演簇豁免）**：原文未明文开场在场的剧情角色，在其剧情首次实际发生前必须保持`待入画`，禁放进建置/实层表/终态名单/差集/速查“等戏”。【实体覆盖】标`入场类别`、`首次剧情Beat`（=剧情触发/入画过程拍本身）、`开场在场依据`。剧情触发节点可拆相邻两拍：当拍开拍不含角色→入戏经具名出入口/Reveal→终态含角色；紧邻下拍才对白/后续主动作，禁止在更早无关Beat预备入场。画外声不算本体入画。仅氛围龙套、群演簇可凭氛围依据开场在场；一旦在画仍守恒。
- **背对镜头禁「对镜头」（最高；Stage 1 §12）**：建置为`背对镜头`/`侧背面朝镜头`时，入戏方向锚**禁止**写`对镜头`（含口型对白与 OS/V.O./旁白/自白）；背对时 OS/内心用`对{后景人}|内心|望天`。要对镜头须先切正面/¾可读角或入戏转正。闭嘴≠可写对镜头。
- **背对/侧背面禁正面才可见内容（最高；Stage 1 §12；含躯干）**：建置为`背对镜头`/`侧背面朝镜头`（含入戏尚未转正时段）时，【建置】【入戏】**禁止**描写须正面/¾才可见的内容——**不仅表情**：①五官微表情/口型/泪线/正面表情词；②**胸膛/胸部/肚子/腹部/正面躯干**及胸前纹饰/法纹正面等；**仅可写**后脑/肩背/后腰/握拳等背面可见面。**须读脸／正面躯干（最高）**：必须写脸或胸腹正面细节时 → **强制**切正面/¾可读角衍生 ENV + **另起新 Beat** + `ENV变=是`；**禁止**同拍背对夹带表情或胸腹正面。侧面仅可写该侧可见半脸/体侧。
- **头眼偏转档位锁（Stage 1 §12）**：约45°–90°侧目/斜睨仅适用于左/右侧面或¾档，且只写该侧可见半脸；背对/侧背面头颈偏转只允许≤约30°并仅写后脑/耳廓/颈侧/肩背，>30°或须读眼神、瞳孔、眉眼、唇角即切可读角或另起转身过程拍。动物身躯F与头部`head_facing`分开核验，身躯背对不可因头扭直接写正面可读。
- **有桌有凳时的站坐安排（Stage 1 §12 强制）**：主环境含桌且配套凳/椅等可坐具时，在姿态必填之上另须裁定站或坐（座姿挂具名椅/凳）；判定顺序：剧情强制 → 礼仪/尊卑/座次 → **无特别限制时优先安排坐下**；禁止无依据全员围桌站聊。**剧情与礼仪允许时**：尽量按桌各边/各向有序分散坐定，并逐人写清相对桌的哪一侧/哪一头、坐哪把具名椅、该向位序及与他人前/后/左/右/距离；**主位/中轴座给地位高者或本拍C位**；禁止只写「坐下/围桌」而无桌向与位序。同场入座超过四人时优先两桌/两区隔离（见「单 Beat 3 人以上分组」）。**有床同锁**：躺/坐须写床头｜床尾｜床沿哪一侧；躺姿默认头朝床头、脚朝床尾。**交通工具内舱不自动适用围坐**（见下条）。
- **交通工具内舱主环境（Stage 1 §11/§12 强制；独立主环境 + 按载具类型惯例）**：汽车内、机舱、驾驶舱、轿子、马车厢、船舱、火车车厢等**一般为独立【主环境】**（自有 0°轴/四向/衍生族），**禁止**并进车外道路/停靠点当舱内衍生。**出入封闭舱**：当场含出入过程时**以仓外为起始主环境，入内后本场切仓内**（`入舱切主环境|出舱切主环境`，一般不分场，禁标反打）。座椅朝向、乘员躯干朝向须**先判该载具类型惯例**，禁止套厅堂围桌对坐，禁止无依据自创相对而坐或全员同向。默认：汽车/客机/驾驶舱=全员同向朝车头/挡风，**司机在前排驾驶座**，乘客=副驾或后排；火车/高铁=先判同向或相对座；**轿子**=舱内通常单人正坐、面朝轿帘开口或行进向，轿夫在轿外抬杠不入舱；**马车**=先判车型（驾位在前朝马；厢式驿车常两排对向长凳；敞篷/骡车常同向或侧坐货厢）。原文特别说明可覆盖，仍须写清座号/哪一排朝哪；代驾仍坐前排驾驶座。同向惯例舱对白默认同向剧情（躯干朝前，可侧目/转头），禁无转身拧成相向凑正反打；机位从舱后拍脸≠改人朝向。Stage 2.1/3 透传座向几何（空镜无人称，驾驶座在前排）；分镜只继承，禁改座向、禁把司机拍到后排、禁入舱后仍挂仓外 ENV 写座舱戏。
- **可移动主体出入画出入口（强制，Stage 1 §11⑧ / Stage 3 §2.2）**：环境设计阶段须为角色/动物/载具等入画/出画预置**具名可通行出入口**（门/门洞/走廊口/巷口等）；Beat 出入画须挂靠该具名出入口（载具驶离可写可核销路径），**禁止**只写「从右边进入」或从实墙凭空出入；**经可开合门/窗时须按开向与门轴侧写过程链**（开门→人通行→带上门→默认关闭终态；剧情明文留门例外），禁跳步只写「推门进来」；Stage 3 须可视化出入口并保留通行净空。入画后正背面另服从上条「入画径向↔正背面硬锁」。
- **运动终点必具体（强制，Stage 1 §12）**：角色/动物/载具等跑/走/冲/驶等运动**禁止**只写「跑向某处/往前冲/朝他跑去/车开走」；须写清终点——具名地点/ENV 结构、具名人旁（方位+距离）、经具名出入口出画外、或画内具体三轴+具名参照落位。
- **多门空间进出（强制，Stage 1 §12/§6）**：目标有≥2门位时，**进入与离开同等强制**——禁裸写「上车/下车/进门/出门/出来」。入=`从{门位}→到{座位/落点}`；出=`从{座位/舱内位}→经{门位}→到{门外落点}`（离座起点+出门扇+落地终点三环缺一失败）；多人逐人写；终态须可检索舱内座号或门外落点；门窗六维须点名具体哪一扇。
- **意图→具体动作（强制，Stage 1 §6）**：【建置】【入戏】**禁止**「试图/想要/打算/企图/trying to」等意图词代替可拍动作；须转译为部位+幅度+始→终位+相对镜头朝向/画幅方向+具体终点或交互对象的可见过程链，并闭合朝向锁/正背面/三联句/运动终点等规则；失手/被拦写可见受阻过程，非「试图失败」；纯心理未执行→思想转自白/OS（明文闭嘴），禁空意图句冒充动作。
- **明确动作 + 效果写到终止（强制；Stage 1 §6 继承并强化上游主笔 9e）**：打斗/法攻禁「混战在一起」摘要；命中须写到位移→撞墙/落地/瘫地或被接住停驻。上游已写终止链→入戏保真禁压成「被打飞」；上游空泛→扩写至停驻（不改胜负/不增改因果）。
- **禁重复招式（强制；Stage 1 §6 继承并强化上游主笔 9h）**：同一连续打斗/法攻段内，同一出手方不得重复或同构再出（同名、同部位同轨迹、同法术介质、仅同义换词）。下一招须换部位/路径/空域技法/介质/命中面之一。挡格可同类但须换方向或部位。上游已差异化→保真；上游重复→改写为可辨换招（不改胜负）。
- **身法与空域技法（强制；Stage 1 §6 继承并强化上游主笔 9g）**：打斗/法攻须写腾空、俯冲、盘旋飞翔等三维轨迹（仙侠等连续段≥2种；禁全程贴地/「飞过去打」）。上游已写→保真；上游空泛→加密空域过程（不改胜负）。无超能题材写地面身法轨迹即可。
- **过程穿插特写表情（强制；Stage 1 §5⑤/§6 继承并强化上游主笔 15c）**：打斗/法攻过程（起势/腾空/盘旋/俯冲/换招/结印）须穿插可读五官或可见面表情特写，禁整段只写轨迹到命中才怼脸。过程表情与命中效果脸并存，不得互相替代。读脸服从背对门禁。上游已写→保真；上游纯轨迹→加密表情切口（不改胜负）。
- **高潮效果双通道引导（强制；Stage 1 §5 / §5-0 继承并强化上游主笔 15g）**：每场写 Beat 前须规划高潮如何用**角色表情或对白** + **环境随剧情递变**（肃杀/明媚/压迫/温煦等）引导；单人场环境通道强制，禁只怼脸/只独白而环境纹丝不动。ENV 基准仍=原文初始态，递变写【入戏】/状态衍生，禁回填主环境。上游已规划→保真不得压扁；上游空泛→场头补规划并加密气质过程（不改因果）。
- **跨拍对白/动作衔接 + 受者响应（强制；Stage 1 §5/§6 继承并强化上游主笔 1g）**：本拍终态=下拍起点；指向他人的对白与交互（递/夺/抓/推/指/出示/扶）须有该受者的应答/可见反应/明文不响应。覆盖率微反应不能替代受者。上游已写→保真禁删；上游缺→扩写补接收（不改因果）。
- **终态落位清晰 + 与周边相对可辨 + 下拍紧密衔接（强制；Stage 1 §1/§6/§12 继承并强化上游主笔 1g A2）**：上拍结束位置须可检索——具名锚 + 在场人哪一侧 + 尺度距离，互不混淆；无走位拍仍须复述相对周边。下一 Beat【建置】抄该终态；要换位须本拍入戏写出过程。禁「走过去/停在那边」无锚，禁下拍瞬移。分镜 P1 与上镜末落位零跳变。上游已写→保真；上游含糊→加密落点相对（不改因果）。
- **成稿总闸：集级因果清晰 + 本集目标完整达成（强制；Stage 1 §44 继承上游主笔 22）**：写完全稿后通读——正文可追因为/所以/但是；本集目标/冲突/转折/小高潮全部可见兑现；禁可拍化冲淡因果、禁删欠本集目标、禁用钩子顶替未完成目标。
- **快慢节奏标注与平衡（强制；Stage 1 掌控配额，对齐上游主笔 1h）**：每个 Beat 首行 `- Beat {n}` = 快节奏交代情节，`~ Beat {n}` = 慢节奏精髓特写（桥段触发/升级/回收、高潮砸点、效果特写、Iconic）。有戏场两种都要；`~`≈30%（20–40%），`-`≥50%；连续 `-`≤4、连续 `~`≤2。**剧本优化拆拍后由 Stage 1 重计并回平衡**；砸点/桥段/效果特写 `~` 锁定不得改 `-`；`~` 不足可升合格精髓拍或拆 `~` 切口，超限可降非锁定过场 `~`（须同步入戏密度，禁只换符号）。禁全快无精髓、禁全慢无推进。分镜继承 Stage 1 成稿符号并**分型运镜+计时适应**：`-`→常速/可快档、短促跟/切、分项下沿、Duration 偏压；`~`→慢档+CU/ECU、Slow Push/钉点、分项上沿、Duration 偏放。
- **特效对实体作用（强制，Stage 1 §8 / Stage 3 §2.5）**：特效/域场须写清作用范围 + 哪些具名固定实体受何种可见作用（灼裂/倾侧/符纹覆盖等）+ 未受影响实体保持原态；禁止只写「能量弥漫」而无实体落点；Stage 3 状态/特效衍生 §C 须逐实体转译。
- **不可见驱动力可视化（强制，Stage 1 §8）**：剧情出现灵气/能量/气/内功/真元/罡气/魔力/念力/力场等本不可见却实际驱动运动或产生影响的力量时，须在对应 Beat 入戏写成可拍可见形态（形态+轨迹+作用关系）；禁止只写「运功/运气」或只写结果而无介质画面；小规模可同拍压缩，大规模叠加特效六相。
- **可拍 ENV**：全部为 `{N}度{主环境名}` 视角衍生（**含强制 `0度{主环境名}` Master，观察轴按 0° 轴锚定优先级；多主环境时每主各一 `0度`**）；Beat **只**指向衍生行——**永远不用裸主环境名作当前环境**（Master 侧必须写 `0度…`）；**一 Beat 一 ENV**（ENV 名变化须另起 Beat）。**当前 ENV 须=本视角直接可见背景半空间**（据【主环境】描述选 N；背景属 0° 扇区→用 `0度…`；属其他扇区→用对应 `{N}度…`；背景锚∈该 N° 扇区）。**衍生环境最小化（Stage 1 强制）**：仅当偏转 **>90°**、越轴、门窗内外/POV/状态·特效等结构切换时另建衍生行；**≤90° 及运镜类变化**（特写/Insert/CU/**同侧过肩**/焦点人物转移）**且角色相对镜头正背面档位不变、背景扇区未变时**沿用父观察侧衍生 ENV，禁止另建。**同场衍生ENV积极换角（Stage 1 强制；≠另建行）**：同一 Scene 内切换合理时须积极按剧情/对白轮次/角色情绪表情改挂已声明不同 `{N}度…`；**原则上同一衍生名连续使用不得超过 3 个 Beat**（可稍后切回，每次连续段仍≤3；满 3 且下一拍合理却仍同挂=失败）。合理=候选角覆盖必见项+对向零可见+守轴+逻辑可拍，禁无动机越轴/机械轮角；满 3 后优先切已声明他角，Insert/CU/同侧过肩仍禁另建专属行但计入连续次数。**OTS读对手 vs 同侧过肩**：越过近侧肩读**对方脸/对手可读面**或背景半空间对向翻转=`OTS读对手/反打`→须切 ENV；近侧肩仅构图框、同半空间、不换对手可读面=`同侧过肩`→沿用父 ENV、禁标反打。**任何形式的反打/OTS读对手必须切 ENV（最高硬约束）**——不论偏转是否 ≤90°，均须 `ENV变=是` 并切换可读角衍生 ENV；禁止反打仍挂上一拍同 ENV；**反打不得跨主环境名**（跨主=`闪回切主环境|蒙太奇切主环境|回切当下主环境|入舱切主环境|出舱切主环境|回切仓外主环境`，**禁混标反打**）。**反打 ≠ 转身（最高）**：反打/OTS 只重投影，画内未转身者 **F 禁止变**；`触发=反打` 却 `F承接.变=是` = 失败。正反打建立镜禁全员正面同 F 同向门；相向双方 F 须≈±180°。**单拍单正背面档位 + 转身另起拍（最高硬约束；Stage 1 §1/§12）**：档位闭集=`正面/¾`（`侧正面`≡¾族别名）｜`左侧面`｜`右侧面`｜`背对/侧背面`；同一 Beat 内【建置】【入戏】`终态`正背面必须同一档——**禁止**同拍既有正面描述又有背面描述；转身/转面/回头转正/转体致档位变 → **强制另起 Beat**（旧拍终态仍旧档，新拍建置=新档）；切镜头/反打致档位变 → 另起拍且 `ENV变=是`；角色自身转身且观察轴未变、背景半空间仍匹配时可同父 ENV（标`触发=转身/转面`）。废止「同拍入戏明文转身同 ENV」例外。**出入画改集合另起拍（最高；Stage 1 §1/§4）**：明文入画/出画致开拍可见集≠收拍可见集 → 过程拍落定后**强制新起 Beat**（新集合为【建置】）；禁同拍续堆对白/主动作；切角暂不可见≠出入画。**对白双方异排异向例外（强制）**：画内双人对话**不同排**或**身体朝向夹角 >45°**（45° 内视为同向）时，须为**各说话人各备**背景衍生 ENV 并在轮次间合理切换。**特写/Insert/CU/ECU/MCU 仅景别收紧、观察轴未变、非反打且正背面档位不变、背景扇区未变时沿用父观察侧衍生 ENV，禁止另建特写专属衍生行**。**特例（绝对必要切视角；须读脸/正面躯干同锁）**：若须给**背对/侧背不可读**的角色做面部或胸腹正面特写，或须写五官微表情/口型（含抬眼/眉尾微挑）**或**胸膛/肚子等正面躯干细节，必须先**另起拍**并**插入/切至其可读角衍生 ENV**（`ENV变=是`），新拍建置已是正面/¾ 后，才可继续写特写/表情/口型/胸腹近镜；禁止同拍 Push In 硬露脸或胸腹正面、同拍背对夹带表情/胸腹正面、或同拍转身兼写新旧两档。
- **转身过程拍纠偏（覆盖上条旧式“终态仍旧档”表述）**：角色自身转身/转面/调头致档位变化，另起专用过程 Beat；建置=旧档/F旧，入戏只写连续转体始→终，终态+速查=新档/F新，转完后不得继续对白/新主动作/新档脸部细节；下一 Beat 建置抄新档。观察轴与背景仍匹配可`ENV变=否|触发=转身/转面`。反打/OTS则未转身者F必须不变，只按新N重投影。
- **180°反打逐人强制换档（最高）**：反打新拍必须以**紧邻上拍终态**为来源；上拍若为转身过程拍，取其终态F新/终态档，不得取建置旧档。`N新=(N旧+180)%360`且无人再转身时，逐人F不变并强制`正面/¾↔背对/侧背面`、`右侧面↔左侧面`，层位前↔后、画左↔画右；轴心可仍中央但档位也须换。自检必填全员`反打档位承接表`与`反打同档人数=0`；任何角色复制上拍正背面措辞即失败。例：N旧=180时F≈0正面，反打N新=0后必为背对，不得仍正面。
- **转身过程语言零载荷 + 终态五件套（最高）**：入戏出现改变躯干F/档位的`转身|转面|回身|调头|转体|完成转身`即强制`拍型=转身过程`；同 Beat 禁`开口|闭嘴|发声|voice_|台词`，新档落定即结束。转身入戏必须写`旋转方向/幅度→新面向目标/方向→新正背面档→新姿态→新世界锚`，切换说明写F旧→F新；裸`完成转身`或缺任一项=失败。下一 Beat 完整建置承接新档后才能说话。若不转身且保持背对，可说话但只能写`发声（口型不可见）`，对白方向须与建置身体朝向闭合。
- **逐人全量自检（Stage 1 §12/§15）**：禁只抽样主角盖章。输入池全员逐人核`N/F/正背面/面向锚扇区/视线/可见面/世界锚承接/姿态/随身件`；本拍所有细节承载实体逐名核`画内+朝镜面+挂载/持握+遮挡`；挂靠固定实体须标`当前角可见交互`或`画外方向锚`，画外锚只许定朝向，禁写成可见背景/细节。任一项失败即`逻辑合理可拍=否`。
- **参考链**：无状态改写时，视角衍生（含 `0度`、N≠0）生图 `visual_dependencies` / `reference_env` → **`ENV:[所属主环境名]`**；**禁止**同基准内角度互挂（如 180° → 0°）；**禁止**闪回主环境衍生挂当下主环境。**状态/氛围衍生（强制）**：跨 Beat 可持续的结构/域场改写，或重大氛围/风格切换（光线、色彩、天地异象等）另建 `{主环境}_{状态}`；其 `reference_env` → 所属主环境或紧邻上一状态。**衍生的衍生（强制）**：状态确立并 `continue` 后，命名如 `{N}度{主环境}_{状态}`。Index `reference_env` 与 Stage 3 `visual_dependencies` **同核** = **同角已切割**衍生（`ENV:[{N}度{主}]`，再状态则挂该角上一状态）。**对应准确（最高）**：依赖行 N 必须与本行相同；禁止挂他角切割图；禁止在已有同角切割时回挂主环境四向拼图。**衍生的衍生生图词须标注所属主环境名（强制）**：`generation_prompt_cn` 须可检索 `所属主环境={Index 主环境名}`（逐字符）；§B 以同角已切割图为本镜头 + §C（**禁止描述未改实体**；变化实体名与原名逐字符相同，不得重新取名），禁止再从四向拼图重切；禁只写泛称、禁串挂他主；2.1 `derivative_base_zh` 仍=该主环境名。
- **跨阶段职责边界（强制）**：**Stage 1/2.1 只声明轻量角度清单**——衍生环境名、`view_angle_from_main`、触发原因（一句话，OTS/反打须含两步确认结论 `N_对手`→`(N_对手±180)%360`）、`spatial_axis`、`lens_profile`（Master/建置=`Wide`；OTS/反打=`Standard`，禁广角）、`axis_crossing`（180°反打=`PlannedReverse`；越轴=`MotivatedCross`）、`empty_view_delta`（可选一句话）；**不写**该衍生的 0/90/180/270 四向具名实体描述或前景/中景/背景 空镜层次。**Stage 3（entity_design_environment_and_poster.md §2.5）是四向对位与美术设计的唯一执行方**：依据 Stage 1【主环境】固定实体清单 + 上述角度/触发/轴线元数据独立完成——**主环境 `generation_prompt_cn` 折中两段式（最高）**：①全局共享（整体风格+开篇拓扑+光影+色彩+构图倾向+中区实体，原有内容不得少）→②**【四面内容基准】**每向**只写后景+中景**（含细节·材质；不写左右缘长文；**每向必须体现构图与纵深方法**：本向三分/压迫落点+近中远受光）→③**【四向拼图】**每格=引用该向后景/中景 + **查表结果句的左右缘钉**（禁「旋转/对照邻面」过程词；禁省略侧缘；左右缘完成围合纵深）；构图与纵深是方法，必须在全局→四面→四格过程中落地，禁止收成一篇单镜头 16:9 空镜顶替 2×2，也禁止骨架在而四面无纵深/构图落点；四面实体用 **【{N}度方向】** 自然语言；视角衍生仍只截取对应宫格，禁另起四面基准。禁键值标签式；**严禁对向串写**（0°↔180°、90°↔270°互不点名对面扇区实体，禁止「不可见{对向实体}（机位后方）」）；**衍生环境景深与左右两侧视野可见性（最高硬约束）**：主环境四向拼图与衍生环境生成须保持约 35mm–50mm 电影级眼高焦段的适度景深与开阔视野，正对输入该向主景（四方正交），画面的**左侧（`(N+270)%360` 侧）与右侧（`(N+90)%360` 侧）的侧壁立面、侧向门窗侧沿、侧边家具或贴墙构件在纵深透视中基本可见**，形成自然的三面三维围合空间感；**跨衍生环境实体描述（含朝向等）规则化变换与逻辑一致性（最高硬约束）**：场内所有实体在主环境【四面内容基准】各向后景/中景中须按几何投影变换落位与朝向；**变换只用于写【四面】后景/中景**；【四向拼图】引用二者并用查表钉句补左右缘；各 `{N}度` 衍生生图不再二次推演楼底，只截取已画好宫格，不做调整；**邻向入境硬约束（强制）**：相邻两向立面作为透视侧立面基本可见入画，但**禁止**把邻向扇区写成平视正对的背景主景（正对完整立面只属于所属扇区格）；**本条不约束中区地面家具**（茶几/矮几/中央桌须四格守恒，见上「中区地面固定家具跨格守恒」）；**四向具名实体须与主环境固定清单逐字一致、同等具体度**，逻辑严格匹配，仅本扇区内遮挡/方向性物体锚点朝向/正反面/左右序位可变；**可数同类实体（椅/杯/窗/柜等）总个数、分边/各向个数、逐具朝向与同侧摆布顺序、逐具区分特征跨主/衍生须一致**（且服从**实体唯一性优先**：默认不重复，门/窗通常仅一具；衍生 §B 只截取对应宫格，不重写排布）；**主环境须声明头尾双锚**（各从四向固定实体择一，分居 Viewing Direction 轴线两端，由 Stage 1 主环境声明）；成稿**优先挂可见头/尾锚**，首选不可见时改挂对向锚；**固定实体世界朝向跨衍生须一致**（状态/特效衍生或明示受力移位除外）；**环境描述/生图词禁人物（最高）**：主/衍生 `environments[]`=`纯空镜`——无人名/人称/站位/交互，**亦禁**「用于某角色干什么」的用途叙事；上游 purpose/activity 仅规划参考，不得写入 `generation_prompt_cn`（见 entity_design §2.6 / common Clean Plate）；**衍生环境 前景/中景/背景 每层每实体须写清上/中/下**（主环境可不写 前景/中景/背景）；**方向性实体类目见 Stage 1 §11「方向性实体参考清单」**。**成稿 vs 内部推演**：转角对照、镜像映射、分层推演、交叉校验、轴线回转公式等仅作内部推演；**成稿方向性物体须锚定四向固定实体（头尾双锚优先）**，头尾均不可见时才用相对镜头/Observer View 视角表述，**禁**成稿写度数回转/公式/轴线回转向；**禁**主环境/转角对照/继承/映射等回指（细则 entity_design_environment_and_poster.md §2.5）。

## Prompt files

- 2.1: `skills/scene_analysis_feature_stack/scene_planning_2_1_assets_extraction.md`
- 2.2: `skills/scene_analysis_feature_stack/scene_planning_2_2_beats_generation.md`
- 3: `entity_design_character.md`, `entity_design_prop.md`, `entity_design_environment_and_poster.md`
- Stage 1 backfill: `project_visual_backfill.color_spectrum` — 基于 `borrowed_films` 的文学级主冷暖方向（四选一：冷调主导|暖调主导|冷暖对比|同温层次）；Stage 3 §1.5 须以其为首要色谱推导输入
