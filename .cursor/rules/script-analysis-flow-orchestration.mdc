---
description: Script analysis flow - Stage 1 subskills and per-scene parallel fan-out
globs: frontend/src/pages/editor/components/ScriptEditor.jsx,backend/app/services/script_analysis_flow/**,backend/app/services/scene_subskill_pipeline_runner.py,backend/app/services/environment_plan_runner.py,backend/app/core/prompts/skills/scene_analysis_feature_stack/**,backend/app/api/endpoints.py
alwaysApply: false
---

# Script Analysis Flow Orchestration

## Scope

本规则约束流程拓扑、并发、持久化、跨阶段接口，以及**各子技能职责边界**。Prompt 只写本环节要做的事与成稿形状；跨环节「不做 / 归下游」不要写进 prompt。禁止在此镜像 Stage 1 建置/投影细则或 Stage 3 四向美术规则。

## Core topology

Stage 1 先执行 `scene_split`（全局统筹），然后**两条主线同时开始**：

1. `environment_plan`（环境规划）：执行前注入项目原有主环境（未删除）。LLM 据全局统筹做场景勘探，识别明示与隐藏的主环境需求（闪回、环境实质改写、舱内外、甲板等体外占用等），只返回按 `scene_id` 包裹的逐场**主环境**补丁；可复用则锁名并由程序继承原定义回填。衍生环境不在此节点。载具整车入他环境只认已抽 PROP，不并进占用空镜；整车无人全景须另有宿主大环境，禁把自身舱/甲板当子环境容纳整车。占用 XOR：有人做场的当前主环境必须是舱/甲板占用，禁把宿主大环境写成有人拍的当前环境。
2. `scene_subskill_pipeline`：Scene 之间并发，单 Scene 内先执行 drama（文戏增强：场级评估 + 特效宽松评估）→ 程序提取 `SPECIAL_SCENE_ANALYSIS` → combat?（武戏增强；VFX 或 XIAN 任一行命中即调用一次，通读全部输入）。**场景现场编排必须等本轮 `environment_plan` 节点成功**，再按场注入主环境，然后 derived_framing → staging。分场旧稿或上轮 adaptation 里的环境块不得提前开闸。建置 LLM **禁止**在该场现场编排 LLM 成功（含【Beat主体定位】【取景锁定】与 `[DERIVED_ENV…]` 提取标签）之前发出。

`scene_split` 完成后立即并行：`asset_design_character`（只消费 `[CHAR_EXTRACT]`）、`asset_design_prop`（只消费 `[PROP_EXTRACT]`）、`environment_plan`、`scene_subskill_pipeline`。`environment_plan` 只读 CHAR/PROP 提取做衔接，**禁止**再输出 `[PROP_EXTRACT]`；完成后立即启动 `asset_design_environment`（**只设计主环境**，封面读程序「封面海报简报」，不等待逐场分析）。该场 `derived_framing` 成功后立即程序提取衍生环境并写入资产库提示词（第一刀只切割；`_仰天`等特别行须写入 `所属主环境=`/`angle_key=`/对应宫格作空间基准，并整句注入现场编排 `特别表述=` 作为变化，禁止只切割），不等待建置或其它场次。该场 `staging` 成功后程序从建置稿直接写入工作区场景表。`assets_extraction` 与 `scene_markdown` 已退役，禁止再走 LLM。

```text
scene_split（切场 + CHAR_EXTRACT + PROP_EXTRACT + 【本场角色】）
  ├─ asset_design_character（只消费 [CHAR_EXTRACT]）
  ├─ asset_design_prop（只消费 [PROP_EXTRACT]）
  ├─ environment_plan（只读 CHAR/PROP 提取做衔接；只出主环境）
  │    └─ asset_design_environment（主环境 + 程序封面简报 → posters[]）
  └─ scene_subskill_pipeline
       ├─ SC01: drama → combat? → derived_framing → staging → workspace Scene
       ├─ SC02: drama → combat? → derived_framing → staging → workspace Scene
       └─ ...

per-scene staging import + linked ENV visual assets ready
  → storyboard_generation for that scene
```

Storyboard 只等待当前 Scene 的工作区导入与 `asset_design_environment`；不等待全部 Scene、character 或 prop。

## Skill ownership

各子技能只写自己的产出。跨环节内容由下表归属，不要在对应 prompt 里再列「本文件不做」。

| 环节 | 产出 |
| :--- | :--- |
| `scene_split` 全局统筹 | 多线叙事、按播放顺序切 Scene（穿插多线/回切各成一场，不按线归堆）、场际衔接（音画/同型/OT/轴锚/状态差、**艺术转出**在前场终场写未完成起势、**艺术转入**在后场开场写落成，分场自足供该场文戏继承；闪回/倒叙/梦境/想象跨入跨出与多线穿插/回切强制锁转出转入，同场短闪回不锁场际改由文戏衔拍；不写进正文、不要求下游对读邻场）、闪回分档、场号配对、每场 `[SCENE_CAST]`（【本场角色】【本场道具】，标准表达）、每场【本场服化道】（按人/按件：入场/变化/离场）、每场【本场姿势】（画内每人开场/入画/变化/离场闭集姿；原文有则摘、缺则按剧情补全，供环境规划锁座椅/床榻/跪地/站位；不改正文）、整集 `[CHAR_EXTRACT]`/`[PROP_EXTRACT]`（**只抽本集出场**，台账全局角色未出场不提、本集已有可见形象的全局角色不漏提；**纯画外音/无形象不提 CHAR**；**照片/视频/画像/屏幕等介质内可辨识面孔也抽 CHAR**，介质物件本身才走 PROP/ENV，禁用相框顶替里面的人；闪回可见仍抽；**有戏份宠物/可持续法相抽 CHAR**（禁降 PROP）；群演簇仅全员无个体动作并写规模/队形/服饰倾向，有动作者按龙套单提，簇附属物不抽 PROP；XOR：可归衣着进 CHAR、**已上身绳/铐/镣等束缚件只进 CHAR 衣着禁升 PROP（本集既缚又解则拆两条 CHAR）**、可归陈设留给环境规划、明文全局/极严+四维/载具整车入他环境进 PROP（甲板/舱内占用另留 ENV；体内体外各自判断，双用途分名并存）、**跨 ≥2 动作节拍持续手持/佩戴/背负/操作的主体进 PROP（禁等用时才抽；纯外形配饰与已上身束缚件不升格）**；一次消耗品/一次性出画不提；文字载体写可见文字+排版+书体+繁简；发声屏写现位+正面朝向；每条独立 PROP 须据使用/挂载裁定 `参照主体` 标准化线性图及对参照的长/高/宽比例并注入道具资产生成，武器对人、配饰对宿主性别+挂载器官；CHAR `身份=` 须完整抽象，含 `现时`/`轨迹`/`曾经`，禁只写现时快照；CHAR 含 `实体类=`/`物种=`/`性别=`/`年龄档=`（非人无明文不强填）与短 `标签=`/`标签_en=`/`标签字体=`/`标签字色=`，上屏物理文字名牌跟项目语言，不是对白字幕；标签默认空、优先空、禁推理，仅剧中明确身份介绍（自我/他人介绍或剧本已写名牌）才填职业/公开身份明面，禁从 `身份=`/称呼/服制/叙述推断，禁剧情发展、角色定位与重生/救世主/穿越等剧情功能词；蒙面/易容等见不到真脸则 `名牌=无`（连裸名也不打），本集稍后揭面才见真脸则 `名牌=揭面后`；剧本已写名牌只留其中明面项）、整集【服化道连续性】、`project_visual_backfill`（继承项目全局风格、光线风格与配乐推荐；`music_recommendation` 含乐器/风格/情绪/节奏/拍型指导，供文戏 `-`/`~` 对齐；场外综合信息总结进 `comprehensive_plot` / `comprehensive_assets`） |
| `environment_plan` 环境规划 | `SCENE_ENV_IDENT`（识别 + 定位/目标/情绪表达）、新建主环境骨架与实体落点（【主环境】四向/固定清单 +【未落清单】开场在场\|待入画）；可选 `背景微动件=`（至多一具细小陈设，不占舞台/动线，供建置/入戏写背景微动；无则`无`）；门窗类锁`扇型=单扇|双开`+初始开闭/`开向=里|外`/`门轴=门左|门右`/把手位（双开一口两扇分锁左右扇开闭/门轴/把手与`通行扇=`；四向所属向与固定清单各写一遍）；可固定参照物无剧情不摊多件；参考图/原定义已锁落位者不重写位置；只读全局统筹 CHAR/PROP 提取与【本场姿势】做衔接（坐→椅、躺→床榻、跪蹲→净空、站→站位，禁与姿势对不上的家具；**已抽 CHAR/PROP 主体禁止进入环境描述、禁止作为环境入画像**，禁同义换称顶替；**本环节锁座椅惯例朝向 + 中区锚/宫格**：主位朝门、客位朝桌心、驾驶座朝仪表盘，固定清单逐具 `朝向=`/`锚=`/`宫格=`（相对|绝对，主语=家具），挂靠小件锁眼高可读承托面；**中区案上台面件本环节锁整数 `数量=` + 台上槽，禁若干，禁把 CHAR 宠物/已抽 PROP 写上台**，下游只继承），**禁止**再输出 `[PROP_EXTRACT]`；复用骨架由程序按识别结果继承原定义回填，复用场仍写本场三属性 |
| drama 文戏增强 | 改编剧本与细节优化（文学视角加厚后落到道具特写/表情特写/宏观场面/音效/空镜 Beat；奇观过程归武戏增强；左右手脚、手机类取用、对白/动作朝向、门开闭（手+接触点+推拉+开向+人站侧+扇展侧））；**动作规范**（操作链/因果链/位移始末/待入画须写经出入口入画（禁鬼魅）/障物评估与绕过（挂物件画左|画右）/投射命中/全员姿态与受力/继承【本场姿势】写入入画与变姿拍/亮屏界面链/**运动舱内写舱位在运动**（舱体晃/窗外过幅/随动/运动音；明文停靠才静）/**仓外大背景切舱内**（街上→车内须写切口+运镜档：跟随入舱/过框推入等，禁街舱混空间）；打出角色名牌物理文字不算动作）；**对话内容**（语气层八键、`{台词}` 花括号强制、方言原文、OS/自白闭嘴、听者微表情、音效；禁裸句、禁「语气层+台词」）；**画外音/VO 单独成行**（可与画面/动作/画内对白同拍，禁遗漏、禁吞进音效或动作叙述）；原文对白/动作/表情/姿势/配乐/音效只增不减；场核 `承接=`、`SPECIAL_SCENE_ANALYSIS`、最终 Beat（每拍 `节拍=`）；**场内艺术衔拍**（同族手法改拍或增拍；增拍=同时转入转出衔接邻拍，≠场际转出/转入；服务卖点/峰谷/氛围）；**快慢节奏**只读 `music_recommendation` 的 `节奏=`/`拍型指导=` 调 `-`/`~` 配额；**【声画综合】**+每拍【配乐】【音效】（`锚=`/`配合=` 用 CHAR/PROP 标准表达，对齐 `Global_Style`+总谱）；**叙述层 CHAR/PROP 标准表达**（口径同 Stage 2.2；禁 ENV）。不输出 `project_visual_backfill` |
| combat 武戏增强 | 通读文戏后的全部输入做打斗/法攻/仙攻/宏观/宏大特效过程加密；不按路由短引提取。文戏 Beat 过程/对白整行（八键+`{台词}`）只增不减。继承 CHAR/PROP 标准表达，禁拆前缀、禁 ENV；加密段按总谱加密【配乐】【音效】，`配合=` 对准出手/结印/受击动作，拆并后回填【声画综合】B 号；**运动舱内**保留并加厚舱位运动（借晃/踩稳/扶座/窗外过幅，禁当静止擂台）；**仓外切舱内**保留切口与运镜档，落定后走舱位运动 |
| derived_framing 场景现场编排 | Beat 正文只增不减，开口行（八键+`{台词}`）整行透传。**总序**：落点三级（明示>分析>避难点）+ XOR（相对锁人在该锚哪一侧，以及这一侧的三维：纵深/画左画右/高低）→ 围桌/舱位锁到槽位（哪一侧第K/M、第N排第K/M）→ 以 `0度{主}` 画 3×3 宫格（仅剧情近距同格）→ 同锚/同绝对格锁 `同簇相对=`（0° 三维；Beat 按本拍 N 投影）→ 同格挤身按景别锁 `部分可见`+`露=`（部位带左右）+ 未结束接触逐拍抄 `触=`（牵手/拥抱/押送等）→ 群演簇原则上独立拍主拍+空格疏离 → 【宫格图】（四周主体+角色面朝文字图+已声明角 `可见面推=`；落点画位≠可见面；走位换格从下拍更新占用/图；换衍生全员 `换算=0→N`）→ 每人 `背对=` 锁 `主拍ENV=` → **有表情禁背对**（本拍正文读脸者 `体侧=` 不得背面，须改挂其 `主拍ENV`/可读¾侧角）→ `朝向=` 仅剧情明示或面向镜头（正对镜头半空间只写 `面向镜头`，禁「即」），非明示衍生环境主体只过程 `朝向推=`（正对镜头禁再写对着大门）→ 据景别/距离/`对打=正反|并排` 挂本拍衍生（景别跟项目横/竖屏侧重；**大全景收中景须见人阶梯** `EWS→WS→FS→MS`，禁空镜起幅后中景突现；**载具占用 XOR**：有人航拍排进舱/甲板/车顶占用衍生、禁直接进宿主大环境，整车入他环境全景挂宿主且无人；**入场双义务**=吸睛+整体建置，特写起幅须拉开再建置）→ §4 机位构图 → §0.5 可见性（相对跟锚、绝对跟层位；`CU|ECU` 仅主拍 V）。R=本拍必见集，已入场非 R 可 `暂不可见`，不为他改挂。衍生 `背景=`/`画左=`/`画右=`/`画外=` 必须严格照抄所属主环境对应向已点名主体（`背景=N度`／`画左=(N+270)度`／`画右=(N+90)度`／`画外=(N+180)度（不可见）`），禁`无`、禁另造、禁 CHAR/PROP/动作。选角与建置/入戏禁止点名 `画外=` 主体。【宫格草稿】含【宫格图】+【主体定位方案】+【Beat主体定位】（含 `体侧=`）+【取景锁定】。CHAR/PROP 逐一核销：开场入方案+B1 全景（含无动作闲人/件），或锁 `入画@B`+`经=`出入口；每拍【Beat主体定位】覆盖仍在场全员；禁鬼魅。画内 V≤4。一拍一衍生。不成稿覆盖矩阵 / 独立【角色组】 / 【Beat景别构图方案】 / 拍内【场记分析】 |
| staging | 在已锁起始帧上写朝向 / 走位 / 落点变化（不改 XOR，除非出入封闭或离开锚）；**【入戏整行继承上游 Beat】**（动作/表情/姿势/开口行八键+`{台词}`/配乐音效只增不减，禁「语气层+台词」占位，台词必须 `{}`）；**【建置】=动作前**（=上拍终态；本拍转身/走位不进建置，新朝向进终态、下拍建置才更新）；转肩只从建置面向+本拍三锚判角色自身左/右。建置只写正面朝向（`面向=` 脸朝近镜头半空间只写 `面向镜头`，与明示对象 XOR）+ 镜头关系（抄上游 `可见面推`/`体侧=`；成稿不明示180°）；`朝向推=`/`背对=` 只进场记（禁重复写背对实体；正对镜头禁再写对着大门或本拍 `画外=` 主体）。建置/入戏/`ENV背景微动=` 禁止点名本拍 `画外=`（镜头后、明确不可见）。**开场全景+无动作仍在场**（B1 先锁应存在的 CHAR/PROP 全景；无动作≠从该拍删人；V 入建置、暂不可见留速查站位朝向；每拍跟踪进出与运动，无走位则朝向站位抄上拍终态）；**建置名单=本拍【Beat主体定位】`可见性=V|部分可见`**（执行定位行序/朝向/距/`同簇相对=` 本拍投影 + `露=`/`触=` + 围桌/舱位槽位：哪一侧第K/M、第N排第K/M+功能；禁补未锁、禁漏、不得自造 X）；**接触未结束则每拍建置复述**（牵手/拥抱/押送的左右手/臂/肩，禁下拍写成独立站位）；跨拍对照已锁可见性：**不缺、不矛盾、不多出**。**建置仍按相对/绝对/封闭**；成稿=结果位 `位于{可见锚}{侧}` **+ 明示姿势闭集**（站/坐/翘脚坐/平躺/侧躺/蹲/跪等，禁只写位于）**+ 开拍手持/佩戴（无则无；禁用时才点名）+ 可见脸表情（脸不可见豁免不写）**；**有表情禁背对**（入戏写神色则不得停在 `背朝镜头`，改建置可读面或回流改挂衍生，禁删表情），禁「相对{锚}」过程词；**建置/入戏不解释环境背景与内容**（交给 ENV 参考图）；具名环境主体只作定位/朝向，不另交视频模型；可见锚优先；宫格参照=`画外=` 则改写 `位于{近镜头中区主体}{侧}旁`（优先 `四周=中:`，禁点画外件、禁拿远景背景顶），无可见锚才写靠近/远离镜头；世界槽位切角不改，镜头纵深必须投影（0°近→180°远）。轴位连续（默认不越轴）。拍摄键写入【建置】，过程写入【入戏】，连续性写入该拍【场记分析】（开拍/收拍在场人数与逐主体宫格/可见性及因=景别|镜头角度，并入取景锁定推导键；程序剥离后不进分镜）；承接文戏动作（不重写左右手脚/取用/朝向对象；门窗/车门/舱门开关按环境规划 `开闭`/`开向=里|外`/`门轴`/`把手` 匹配改写推拉·接触点·扇展，双开点左扇|右扇|双扇逐扇核销）；逐拍出入画闭环（执行上游入场核销，禁鬼魅）；正反/OTS 建置；ENV 背景微动；首登名牌不算动作，只跟该人入戏主动作后。场头只写短【实体覆盖】 |
| `assets_extraction` | **已退役**。封面改由环境设计读程序「封面海报简报」 |
| `scene_markdown` | **已退役**。staging 成稿由程序提取后直接写入工作区场景表 |
| `asset_design_character` | 全局统筹 `[CHAR_EXTRACT]` → `characters[]`；用户侧只注入项目信息 + 角色提取，**禁止注入待分析剧本与道具信息**；全局统筹一完成即可开写；不等待资产清单、环境规划或逐场分析；Index `character` 行只作旧稿兼容；身份轨迹须进定妆（原富贵后落寞 ≠ 一直贫困）；服饰特征槽须全覆盖并写入 `generation_prompt_cn`【衣着】穿戴句（键值不算落地）；衣着中的绳/铐/镣等束缚件须画进定妆、禁止另造 PROP；缚/解两态各出一条换装衍生；成人年轻女性按风格取性感特征集写入 `性感落点=`（影游禁时装深V短裙顶替）；人着机甲/盔甲等战斗服须 `防护落点=`+`进攻落点=` 同时落地（整机 PROP 不进定妆）；神兽/异兽/妖兽等须 `兽种=` 全槽落入【相貌】（被覆/装具进【衣着】） |
| `asset_design_environment` | 环境规划 IDENT/【主环境】→ 主环境 `environments[]`；封面读程序「封面海报简报」写 `posters[]`；用户侧只注入项目信息 + 封面海报简报 + 环境规划，**禁止注入待分析剧本**；不等待逐场分析；衍生由现场编排后程序入库；**已抽 CHAR/PROP 禁止写入环境描述、禁止画进环境空镜**（规划误点名须删件，禁同义换称入画）；**中区默认空区无障碍**，仅规划明文桌椅等才落，禁止擅自增加主体；门窗世界合同继承规划，四宫格须译成各格扇展（侧壁铰链用近端/远端，禁把门轴世界=门左抄成画面左）；场所/建筑风格槽须全覆盖并写入开篇+四面后景画面词；殿宇类天花须点名经典建筑术语库原词（影游默认哥特式高挑穹顶）；座椅 F 与楼梯 U 分表（光学相反）；座椅 F 继承环境规划 `朝向=`（主位朝门 / 驾驶座朝仪表 F=0），本环节不改惯例只译可见面；0°/180° 椅面不得同朝镜头；**中区椅/沙发/轮椅/几须继承规划 `锚=`/`宫格=`（现场编排同套 XOR+3×3，主语=家具），四面只写切角投影落位+可见面，禁一格蒸发、禁换墙、禁四格照抄「某墙边」；挂靠小件随宿主入镜，禁顶部出画死角只出现一格；**中区台面可数件四格同核开篇 `数量=N`，禁一格蒸发/改数/同款克隆** |
| `asset_design_prop` | 全局统筹 `[PROP_EXTRACT]` → `props[]`；用户侧只注入项目信息 + 道具提取，**禁止注入待分析剧本与角色信息**；全局统筹一完成即可开写；不等待资产清单、环境规划或逐场分析；Index `prop` 行只作旧稿兼容；机甲/机车/军械/军甲/舰艇/战机等须器类全槽写入结构/工艺画面词（键值不算落地）；第一/第二宫承接提取块 `参照主体=名=` 与 `长=`/`高=`/`宽=` 及 `尺度=`（空才自拟）且与道具完全隔离，描点须含参照物全名及对参照的长高宽比例与数字单位，线性图上须有同核可读文字 |

程序接口：drama 完成后提取 `SPECIAL_SCENE_ANALYSIS`；`[VFX]` 或 `[XIAN]` 任一行命中则调用一次武戏增强。`SCENE_ENV_IDENT`（含定位/目标/情绪表达）与主环境实体落点只由环境规划产出；CHAR/PROP XOR 与提取只由全局统筹完成。

## Registry source of truth

文件：`backend/app/services/script_analysis_flow/registry.py`

- `scene_split.fan_out`：`environment_plan`、`scene_subskill_pipeline`、`asset_design_character`、`asset_design_prop`
- `scene_split.fan_out_mode`：`parallel_after_complete`
- `environment_plan.fan_out`：`asset_design_environment`
- `assets_extraction` / `scene_markdown`：`enabled=False`，`status=retired`
- `asset_design_character.depends_on`：`scene_split`
- `asset_design_environment.depends_on`：`environment_plan`
- `asset_design_prop.depends_on`：`scene_split`
- `storyboard_generation.depends_on`：`scene_subskill_pipeline`、`asset_design_environment`
- `stage3_auto_start`：分别控制三类资产和 storyboard 自动启动

## Frontend and backend

- `executeAdvancedAnalysis`：全局统筹后并行角色设计、道具设计、environment_plan（环境规划）与 scene_subskill_pipeline（文戏增强 → 场景现场编排 → 建置入戏）；角色设计只注入项目信息 + `[CHAR_EXTRACT]`，道具设计只注入项目信息 + `[PROP_EXTRACT]`，二者均不注入待分析剧本且互不注入对方信息；环境规划一完成即启动主环境设计（只注入项目信息 + 程序封面简报 + 环境规划，不注入待分析剧本）；该场 staging 完成后程序+前端直接入场景表，不再跑 2.1/2.2 LLM。
- 进度诊断拓扑：全局统筹 →（角色设计 ∥ 道具设计 ∥ 环境规划 ∥ 逐场优化）→ 主环境设计 → 建置稿入场景表 → 分镜。
- `runPostImportSceneSubjectPipeline`：角色、独立道具与主环境均可在无 Subject Index 时启动；ENV 成功后 flush storyboard queue。
- `analyze_scene` 资产设计节点禁止再以 Subject Index 作硬门槛；角色/道具/环境分别读提取块或环境规划。
- 环境节点完成只认主环境已入库；仅有衍生环境不得算完成，也不得打开分镜 ENV 门闩。
- 资产重跑真源：角色看 `[CHAR_EXTRACT]`，道具看 `[PROP_EXTRACT]`，主环境/封面看环境规划（`SCENE_ENV_IDENT` / 【主环境】 + 程序封面简报）。禁止再把 Subject Index 当作重跑硬门槛。
- 「重生衍生环境」只读各场 `derived_framing`（`scene_subskill_results.framing`，缺则 staging / pipeline `scene_block`）程序入库，禁止走 Stage 3 LLM。API：`POST /prompts/scene-analysis/flow/ingest-derived-environments`。
- `handleRestartStage2`：跳过资产清单/场景编排 LLM，从建置稿重导入。
- `tryResumeAnalysisFromExistingArtifacts`：缺 scenes 时从 staging 入表；仅 subjects JSON 不完整时只补 assets。
- Scene 导入成功：queue/kick `kickoffStoryboardForImportedScene`，并更新 `storyboardTaskProgress`。
- `run_scene_analysis_flow_node`：单节点执行；`scene_subskill_pipeline` 节点内部负责按场并发及场内串行，并在 staging 后 upsert 工作区 Scene。
- API：`POST /prompts/scene-analysis/flow/run-node`；重生衍生环境：`POST /prompts/scene-analysis/flow/ingest-derived-environments`。

## Change constraints

1. 禁止再把新阶段挂到已退役的 `assets_extraction` / `scene_markdown`。
2. main / restart / resume 路径的并发拓扑必须一致。
3. `explicitSubjectIndexText` / `parallelWithScenes:true` 不得依赖 Scene 已导入。
4. 并行分支启动前先置 `environmentAssetDesignPendingRef`，防止早到 Scene 读取陈旧 episode JSON。
5. 每个 Scene 导入后进入 storyboard queue；必须等本场所属**主环境**已入库且 `generation_prompt_cn` 非空后才能开闸，再注入该主环境提示词。仅有衍生环境提示词、库里残留主环境行、或本轮尚未规划环境，都不得开闸。无环境需求且 ENV auto-start 关闭时才可跳过。等待态=`waiting_env`。
6. character/prop 完成不得打开 ENV gate；流水线末尾只 residual-kick 尚未启动且 ENV gate 已开的 Scene。
7. Storyboard 注入 `# Scene Subject Image Prompts (CN)` 仅含当前 Scene 关联 ENV 的 `generation_prompt_cn`；Subject Index 可继续列 CHAR/PROP/ENV 供命名。
8. 手动 force storyboard 仍须验证本场所属主环境已在实体库且 `generation_prompt_cn` 非空；禁止只凭衍生行开闸。
9. Scene marker parser 与 Stage 1/2.1/2.2 同步：接受成对 `[SCENE_START:EPxx_SCyy]` / `[SCENE_END:EPxx_SCyy]`；`SPECIAL_SCENE_ANALYSIS` 由文戏增强产出并写在 Scene 前，程序在 drama 之后提取；`[VFX]` 或 `[XIAN]` 任一行命中则调用一次武戏增强。场外综合信息由全局统筹总结进 `project_visual_backfill.comprehensive_plot` / `comprehensive_assets`，经项目尾注入各场；若旧稿仍含 `COMPREHENSIVE_INFO` 则继续当作只读全局上下文。`SCENE_ENV_IDENT` 由环境规划产出。CHAR/PROP 提取由全局统筹完成；封面清单不再落 CHAR/ENV/PROP 行。
10. 注入 Stage 2.1/2.2、写入工作区场景表、以及分镜前，只剥离成对 `────【场记分析】────`…`────【场记分析结束】────`（场级或拍内皆剥；兼容旧稿 `────【Beat切换说明】────`…`────【Beat切换说明结束】────`）；禁止无结束标记的贪婪剥离。拍内场记分析只服务建置连续性，不传递给分镜生成。

## Prompt interface invariants

- 场景表：由程序从 staging 成稿提取（`scene_id` / `【场景名称】` / Beat=【建置】+【入戏】，剥【场记分析】与【取景锁定】 / CHAR/PROP/ENV 标准表达），不再经过 Stage 2.2 LLM。分镜 `# Core Scene Info` 只注入该视觉指导。
- 命名：`CHAR:/PROP:/ENV:`、JSON `name/name_en/base_name_en`、`visual_dependencies` 必须与 Subject Index 对应中英名逐字符一致。
- Index 外语义：保留 Stage 1 自然语言并在覆盖核销标缺口；禁止自造 `EXTRA:`/`LOCATION:`。
- CHAR/PROP 换版：先按注入的 `form_continuity`（哪场/何时），再按 `applicable_scenes` 与可选 `activation_beat` / `activation_evidence/variant_delta` 核销；无匹配用基础版。
- ENV：只标现场编排已识别的衍生环境名；不得把环境内部件单切成 ENV。环境资产设计不读 Index environment 行。
- Storyboard：当前 Scene 的 Subject Index 是命名与衍生白名单真源；ENV CN 仅供光学锚定。

## Canonical prompt map

- Stage 1 只读基线：`scene_planning_1_script_optimization.md`
- Stage 1 子技能链：`scene_planning_1_subskill_cut_transition.md`（全局统筹：切场+CHAR/PROP 提取+短标签+字体字色物理文字名牌+【本场角色】+【本场姿势】） → `scene_planning_1_subskill_environment.md`（环境规划：场景勘探+识别+定位/目标/情绪+实体落点+主环境；只读 CHAR/PROP 与【本场姿势】） → per-scene `drama_standardization`（文戏增强：改编+细节优化+场核承接+特效路由+节拍类型+姿势入 Beat） → `combat?`（武戏增强） → `derived_framing` → `staging_env`
- Stage 2.1 Subject Index/XOR/衍生字段：`scene_planning_2_1_assets_extraction.md`
- Stage 2.2 Beat 落表：`scene_planning_2_2_beats_generation.md`
- Stage 3 共通：`entity_design_common.md`
- Stage 3 ENV 四向与依赖：`entity_design_environment_and_poster.md` §2.5
- 分镜 Logic/Video：`shot_generation.md`

成稿口径：Stage 1 拍摄键进【建置】【入戏】，推导与连续性进拍内【场记分析】（开拍/收拍在场核销；程序剥离后不进分镜）；除画面句外为结构化极简。Stage 3 `generation_prompt_cn` 与分镜 `Video Content (CN)` 按各自视觉叙述合同；分镜 Logic 为极简标签。建置与 `Video Content (CN)` 均不解释环境背景与内容（ENV 参考图承担）；具名环境主体只作定位/朝向，不进 `Associated Entities`、不另交视频模型。转身过程拍统一为`建置=旧档｜终态=新档`。
