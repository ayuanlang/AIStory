---
description: 剧本分析全局记忆——CHAR/PROP 抽取权威与已退役环节
alwaysApply: true
---

# 全局记忆：抽取权威

- **依赖参考图的不写外形**：文戏加强、武戏加强、现场编排、建置与入戏、生成分镜里，只有依赖参考图的主体不写外形：角色、道具、环境内的主体，以及同类已有参考图的具名物。这些只写已注册名字，不写颜色、形状、大小、材质、纹样，也不改外形。不依赖参考图的，包括特效，可以写颜色、形状、大小和纹样。依赖参考图的外形在美术指导的参考图里（角色定妆、道具美术、环境资产）。
- **角色／道具抽取只在全局统筹**：`scene_split` → `scene_planning_1_subskill_cut_transition.md` 的 `[CHAR_EXTRACT]` / `[PROP_EXTRACT]`。改抽取闸只改这一份。
- **`_archive/2026-09-05-retired/scene_planning_2_1_assets_extraction.md` 已退役**（资产清单／Subject Index 抽取不再走 LLM）。禁止再改该文件的 CHAR/PROP 抽取闸，禁止把它当活权威。
- 角色设计只消费全局统筹 `[CHAR_EXTRACT]`，不等待 2.1。
- **法相／龙／金身／化龙凤**：跨 ≥2 Beat（动作可不相邻，间隔拍仍算）＋正反面＋自身动作 → 必须抽 CHAR，与宿主分名；不另要「可持续身份」；单拍无定形介质不抽。
- **虚实介质 XOR（防视频提示词双人）**：全息人像／幻影等占空间虚拟体 → 单独抽 CHAR（`实体类=虚拟`，与宿主分名，外形含虚像辨识）。屏幕／镜中平面像 → 与视频终端／镜合成 PROP，`依赖=CHAR`，不另起第二 CHAR。同核名禁止在提取、文戏、分镜里写成两个身体同时出现。照片／画像仍抽 CHAR（脸须可印）。
- **PROP/ENV 标志跟道具身份**：主体/手持 → `PROP:` 整物名；容器环境 → 占用 `ENV:` 空间名（车厢/内舱/甲板）；兼则分名并存。`整车PROP=` ≠ `舱内ENV=` ≠ `挂场ENV=`；禁同核名跨标志。环境规划逐字抄预留名，不另起近义。
- **PROP 不替代 ENV**：PROP 是空间里的物件。作 PROP 必须另有宿主/占用 `ENV:`。舱内禁 `坐在 PROP:[飞行器] 的主驾驶座`；整车 PROP 只挂街/空/海，禁挂自身驾驶舱。整机主拍禁 `当前环境=飞行器驾驶舱`；过框拉出落幅已见整机则 ENV 必须已是宿主。
- **整机与舱内角色禁同 Beat**：两套环境，顾此失彼。舱内拍只挂驾驶舱+人，禁整机 PROP／「作为舱体容器」。整机拍只挂宿主+PROP，禁驾驶座 MCU。两边都要 → 拆两拍。
- **整机换 ENV 只在现场编排改档**：整机主拍（`主拍对象=PROP:[飞行器]`、EWS、机位锚=空域/楼群）必须重锁 `载具档=道具沿用宿主|整车无人`，`当前环境` 主名段=宿主。上拍占用、本拍整机 → `ENV变=是:整机换主|出舱切主环境`。建置一眼整机+舱 ENV → 回流，不照抄。
- **环境主体含暗示承托**：环境规划把剧本点名的环境主体，以及动作暗示的承托，具名落入该动作所在的那一块主环境（坐下而未写坐地→椅／凳／榻，人数不少于同时落座人数；躺→床／榻；伏案→桌／案；舱内座只进舱内）。现场编排的参考主体只挂这份名单里的件，或缺件回流环境规划。已抽 CHAR／PROP 不在环境里再造同核物。
- **主舞台区只在环境规划锁**：【活动空间】须写 `主舞台区=` + `舞台范围=` + `舞台大小=纵深=`/`开间=`（闭集=`贴身|一臂|两步|三步|五步`）+ `可容=` + `舞台净空=无障碍可通行`。区内禁立体障碍，动线连通出入画口。环境资产设计逐字继承范围/大小/净空，只把步档译成 `舞台占地=` 米，禁改档、禁往舞台内加障碍。现场编排角色站位必须落在该区内（`锚=`=挂靠或区内近件，`距锚=`不超过对应轴大小）；走位走规划动线。
- **现场编排锁世界物理，禁九宫**：类似环境资产，先按剧情把角色与道具钉在主环境东南西北上，不写画布。定位只三档：①近处有参考主体（环境主体／另一人／独立道具）→ 先选定该主体，再写明相对它的哪一边（世界侧＋`距锚=`）或它身上的哪个点（`触点=` 可指认部位）。只写主体名不算选定。环境资产 `主体分组=` 的每个成员同样写明相对核心的哪一边或核心上的哪个点，只列成员名不算落位。锚必须是该环境内独一、轮廓清楚、并落在本拍剧情所看方向上的单体；禁止用立面／隔墙／墙角／地板当锚，朝向所对的独一件优先（穿衣镜面向床 → 锚=该床）。参照物同时看距离和功能附属，附属单向：椅、凳附属所配的桌，床头柜附属所配的床，手持物附属拿着它的角色；宿主不回头以附属件为参照，禁止互相依赖；②空旷、舞台内无近处参考主体 → 相对世界物理原点（`原点=中心｜北0东0`）锁 `北=`／`东=`／`距原点=`，禁把整片沙／草／天当锚，也禁为了有锚去新造沙丘；③封闭驾驶舱 → 按已锁几排几位、座朝向、与驾驶位的前后左右关系落人，不改排、不改朝向。角色与道具世界位跨拍连续：每拍逐人逐件核销 `在场=` 与位置变，无走位／离身／转身／取出不得改，上拍在场者不得无离场声明而消失。初始位置、本拍走位、走完后的落点都由本拍剧情动作决定：从桌上取物的人必须在桌旁；与开拍位置不一致时，本拍正文先补走到该具，再做该动作。衍生环境的望向要能看见这一动作所用的那一具。每个 Beat 重评镜头并全文重写机位、相对参考主体的边、距离、机高和俯仰；无剧情改机则继承上拍镜头位置，以及角色相对参考图已锁的边或点。禁止机位写成承上。无变化也必须把上拍方式、站位、朝向、姿态、触、手持宿主全文再抄一遍，禁止整行或站位只写「承上」「同上」「见上拍」。同一人、同一件上下拍动作或位置对不上，在本拍该动作之前补过渡或从上一落点走到本拍落点的移动，分布图开拍仍是上拍终态并写 `站位变=是`。灵魂出窍、元神离体、分身、残影，以及特效技术造出的另一具，与本体分名，不按断续补走位。人／件占地不得与已锁环境主体占地重叠，只许沿已锁触点共享边。**禁止** `宫格=`／`西N列×北M行`／后左前中／九宫交叉位。现场编排禁画左画右／离镜头。
- **建置与入戏只按角度翻译角色与道具**：不重做环境四宫。环境仍由该拍衍生参考图承担。参考主体与封闭舱：本拍机位角度把世界位旋成画左↔画右和离镜头远近。世界原点、尤其开放环境：同一旋转，左右仍是画左↔画右，深度写成前景／中景／远景（原点深度=中景，靠近镜头=前景，远离镜头=远景）。景别决定入画体量，景深不改层。建置与入戏的在场名单严格等于该拍分布图，不得增删人或件。同一人、同一件上下拍位置或动作不接时，入戏补上过渡或从上一落点走到本拍落点的移动，建置仍写上拍终态。灵魂出窍、元神离体、分身、残影，以及特效技术造出的另一具，与本体分名，不按断续补走位。描述角色与道具位置时只写画布坐标：参考主体与封闭舱写画左／画右／对齐和离镜头，原点与开放环境写画左／画右／对齐和前景／中景／远景；禁止在位置句里写东／南／西／北。每条建置把译出的画布坐标全文写出，禁止只写「承上」。每个 Beat 按当前衍生环境重评每个角色与道具：就近参考物在本角不可见时，贴着它的人与件通常暂不可见、不进建置；离挂靠较远或体量伸出仍可见必须写明。分镜视频提示词只写该拍建置里的可见者，暂不可见的不提及；换衍生环境后建置重新写入的，从那一拍起才写。同环境后续 P 也照抄该拍建置，禁止只写「承上」，也不把东南西北写进分镜。
- **画布坐标系（四宫与建置同核）**：环境四宫的画面原点=主舞台区心（`原点=中心｜北0东0`）。轴=画面左↔右＋远离镜头↔靠近镜头。开篇资产须锁各件 `触点=`。环境规划即使广袤也必须有一具可定位挂靠（造特异沙丘／草丛／云等），供环境自身定位；人站在空地上不因此改挂该挂靠，仍走世界原点。
- **Beat 规划两层保障**：第一层=文戏增强，写正文前锁【道具身份】`落地=` 与【Beat切分确认】`规划=`。第二层=现场编排，文戏未拆整机/舱内同拍时**必须补拆**（`拆拍依据=占用换主`）。建置不代划，回流现场编排。
- **商业宣传片单场（最高）**：项目为商业宣传片（`type=商业宣传片` / `source_promo_project_id` / `[商业宣传片]` 注入）→ 全局统筹**不分场**，全剧恰好一场；吸睛-共鸣-价值-收口是场内节拍，不因时/空/光变切场。策划信息只在全局统筹注入并继承。
- **季节／时段／时辰／气候／氛围只在全局统筹锁**：`scene_split` → `scene_planning_1_subskill_cut_transition.md` 的【本场时地】。项目 `season_occurrence`／气候天气／Tone 与剧本场头正文已有则继承；时间最低到清晨|白天|中午|黄昏|上半夜|下半夜，并锁十二时辰；气候锁晴|阴|雨|雪|雾|风|霾 + `气候可见`；氛围锁主情绪+空气感+光色倾向。跨光变档必须切场（**商业宣传片除外，仍单场**）。环境规划抄入 IDENT `时地=` 与【主环境】同名字段，`情绪表达` 与统筹氛围同核。环境资产设计只消费、禁改判：时辰→行星/`Key世界向`，气候可见→空气与湿迹，氛围→光色。
- **基础风格模式只在全局统筹锁**：项目 剧本模式（`base_positioning`，与基础风格模式同闭集）有则作主风格；每场写【本场风格】，可按本场空间/时代改选闭集（赛博朋克／近未来科技／古装战争等）。环境规划抄入 IDENT `风格=`；资产设计与分镜「风格定位」只消费本场模式，禁另起冲突世界视觉。
