---
description: 剧本分析全局记忆——CHAR/PROP 抽取权威与已退役环节
alwaysApply: true
---

# 全局记忆：抽取权威

- **角色／道具抽取只在全局统筹**：`scene_split` → `scene_planning_1_subskill_cut_transition.md` 的 `[CHAR_EXTRACT]` / `[PROP_EXTRACT]`。改抽取闸只改这一份。
- **`_archive/2026-09-05-retired/scene_planning_2_1_assets_extraction.md` 已退役**（资产清单／Subject Index 抽取不再走 LLM）。禁止再改该文件的 CHAR/PROP 抽取闸，禁止把它当活权威。
- 角色设计只消费全局统筹 `[CHAR_EXTRACT]`，不等待 2.1。
- **法相／龙／金身／化龙凤**：跨 ≥2 Beat（动作可不相邻，间隔拍仍算）＋正反面＋自身动作 → 必须抽 CHAR，与宿主分名；不另要「可持续身份」；单拍无定形介质不抽。
- **虚实介质 XOR（防视频提示词双人）**：全息人像／幻影等占空间虚拟体 → 单独抽 CHAR（`实体类=虚拟`，与宿主分名，外形含虚像辨识）。屏幕／镜中平面像 → 与视频终端／镜合成 PROP，`依赖=CHAR`，不另起第二 CHAR。同核名禁止在提取、文戏、分镜里写成两个身体同时出现。照片／画像仍抽 CHAR（脸须可印）。
- **PROP/ENV 标志跟道具身份**：主体/手持 → `PROP:` 整物名；容器环境 → 占用 `ENV:` 空间名（车厢/内舱/甲板）；兼则分名并存。`整车PROP=` ≠ `舱内ENV=` ≠ `挂场ENV=`；禁同核名跨标志。环境规划逐字抄预留名，不另起近义。
- **PROP 不替代 ENV**：PROP 是空间里的物件。作 PROP 必须另有宿主/占用 `ENV:`。舱内禁 `坐在 PROP:[飞行器] 的主驾驶座`；整车 PROP 只挂街/空/海，禁挂自身驾驶舱。整机主拍禁 `当前环境=飞行器驾驶舱`；过框拉出落幅已见整机则 ENV 必须已是宿主。
- **整机与舱内角色禁同 Beat**：两套环境，顾此失彼。舱内拍只挂驾驶舱+人，禁整机 PROP／「作为舱体容器」。整机拍只挂宿主+PROP，禁驾驶座 MCU。两边都要 → 拆两拍。
- **整机换 ENV 只在现场编排改档**：整机主拍（`主拍对象=PROP:[飞行器]`、EWS、机位锚=空域/楼群）必须重锁 `载具档=道具沿用宿主|整车无人`，`当前环境` 主名段=宿主。上拍占用、本拍整机 → `ENV变=是:整机换主|出舱切主环境`。建置一眼整机+舱 ENV → 回流，不照抄。
- **九宫是现场编排内部台账**：四周格=主环境四周，中间格=主环境中间；只记相对落点，**不是面积**（中间可远大于四周）。逐人锁 `方式=`、禁混写：封闭舱=排次；中区有主体或人靠边=相对；仅四周只有远山/路尽头等不适合做角色参照才绝对。**三种方式现场编排都禁**前中后景／画左画右／近远镜头。绝对只写 `侧=东|南|西|北`＋`偏=`＋宫格。前中后景、远近镜头、画左画右 **只在建置**按本拍机位＋衍生 ENV 转化。
- **Beat 规划两层保障**：第一层=文戏增强，写正文前锁【道具身份】`落地=` 与【Beat切分确认】`规划=`。第二层=现场编排，文戏未拆整机/舱内同拍时**必须补拆**（`拆拍依据=占用换主`）。建置不代划，回流现场编排。
