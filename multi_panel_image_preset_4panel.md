# 四画格多画格预设

这个预设不是模型官方内置模式，而是应用层对 GPT Image 类模型的一种构图约束。

## 中文指令

```text
生成一张 2x2 排布、包含 4 个连续画面的多画格叙事图。四格需保持同一主体、服装、场景与镜头语言连续，展示该镜头的关键动作推进；不要添加标题、对白气泡、说明文字或无关装饰。
```

## 使用方式

把这段指令拼接在镜头的视频提示词之后，用图片模型生成一张单图。

## 目标

- 输出的是一张单图，不是 4 张独立图片
- 图内有 4 个连续画格
- 四格之间保持角色与场景连续性
- 适合作为应用层的 multi-panel image preset，而不是“官方分镜模式”
