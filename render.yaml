services:
  # Backend Service
  - type: web
    name: aistory-backend
    env: python
    region: oregon
    healthCheckPath: /healthz
    buildCommand: |
      cd backend
      pip install --no-cache-dir -r requirements.txt
    startCommand: |
      cd backend
      python add_users_credits_column.py || true
      python add_scene_ai_shots_result_column.py || true
      python add_users_email_verification_columns.py || true
      python backfill_user_verification_for_active.py || true
      uvicorn app.main:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips='*' --workers ${WEB_CONCURRENCY:-1}
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: SECRET_KEY
        generateValue: true
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: 43200
      - key: SECURITY_HEADERS_ENABLED
        value: "1"
      - key: SECURITY_HSTS_SECONDS
        value: "31536000"
      - key: GZIP_MINIMUM_SIZE
        value: "1024"
      - key: WEB_CONCURRENCY
        value: "1"
      - key: DATABASE_URL
        fromDatabase:
          name: aistory-db
          property: connectionString
    disk:
      name: uploads
      mountPath: /opt/render/project/src/backend/uploads
      sizeGB: 1

  # Frontend Service
  - type: web
    name: aistory-frontend
    env: static
    buildCommand: |
      cd frontend
      npm ci --no-audit --no-fund
      npm run build
    staticPublishPath: frontend/dist
    routes:
      - type: rewrite
        source: /api/*
        destination: https://aistory-backend.onrender.com/api/*
      - type: rewrite
        source: /uploads/*
        destination: https://aistory-backend.onrender.com/uploads/*
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: aistory-db
    databaseName: aistory
    user: aistory_user
    region: oregon
    plan: free # Note: Free tier Postgres expires after 90 days on Render
